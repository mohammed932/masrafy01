<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- DEBUG ONLY. The dev backend is plaintext http on port 3000: `10.0.2.2`
         from the emulator, the host's LAN address (which changes per network)
         from a physical device. Never shipped: this file lives under src/debug,
         so release builds keep Android's HTTPS-strict default. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
