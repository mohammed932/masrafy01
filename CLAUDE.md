# masrafy01 Development Guidelines

Auto-generated from feature plans + constitution. Last updated: 2026-08-13

## Project Identity

**Masrafy** (internally "Credit Match") — Egyptian fintech loan comparison marketplace. Connects users with 20+ bank loan programs (ABK Egypt + partners) via 5-step wizard + matching engine. Four product lines: personal loans, car loans, mortgages, business loans. Free for users; commission revenue from banks. Three platforms governed by a single constitution: NestJS backend (active), Angular admin dashboard (active), Flutter mobile app (deferred until Figma).

Constitution: [.specify/memory/constitution.md](.specify/memory/constitution.md) v15.1.0

**Product scope-lock (v16.0.0 / Principle II):** Platform supports exactly FOUR retail loan categories — `personal`, `car`, `mortgage`, `business`. **The no-payslip product is an income BASIS, not a category.** v15.0.0 shipped it as a fifth category (`fast`, "Fast Loans"); v16.0.0 removed it — the same catalog name is sold against a payslip by one bank and against a grade table by another, so a category duplicates every sellable name and asks the customer to choose between two descriptions of one loan. What the bank reads is `bank_program.programType` (`income_proof` / `income_surrogate`), chosen per program; the applicant is asked the four surrogate facts *as well as* the payslip questions. **No category constrains the program type** — `PROGRAM_TYPE_INVALID_FOR_CATEGORY` is deleted. **No hardcoded capable list either:** `SURROGATE_REQUIRED_CATEGORIES` and `SURROGATE_CAPABLE_CATEGORIES` are both DELETED (not renamed). A category can sell a no-payslip program exactly when its applicants are asked one of the four facts (`question_loan_category`) — admin-configurable on `/questionnaire/categories`, with `personal` + `car` seeded as the default, so widening the product is an admin action, not a release. Removing a category requires a destructive migration that physically wipes registry entry, bank programs, and all applications + cascade (offers / decisions / activities / documents). Ghost / soft-deactivated rows = review block. Adding a FIFTH, or re-introducing no-payslip as a category, requires a constitution amendment (A26).

## Active Technologies
- Node.js 22 LTS + TypeScript 5.6+ (`strict`, `noImplicitAny`, `strictNullChecks`, `noUncheckedIndexedAccess`) on backend; Angular 18 + TypeScript 5.4+ (same strictness profile) on admin. (002-bank-programs)
- PostgreSQL 16 (Prisma migrations only; `db push` forbidden in production); Redis 7 (rate-limit + future audit-event buffer; NOT used as primary store for bank programs). (002-bank-programs)
- Node.js 22 LTS + TypeScript 5.6+ (`strict`, `noImplicitAny`, `strictNullChecks`, `noUncheckedIndexedAccess`) on backend; Angular 18 + TypeScript 5.4+ on admin. (003-matching-engine-post)
- Node.js 22 LTS + TypeScript 5.6 (`strict`, `noUncheckedIndexedAccess`) · Angular 18 + TypeScript 5.4 · Flutter/Dart 3 (mobile) + NestJS 10, Prisma 5, `class-validator`, `@nestjs/swagger`, `decimal.js` (via `@prisma/client/runtime/library`) · ng-zorro-antd (`nz-*`) + `@angular/localize` on admin · flutter_bloc + Freezed + `auto_route` + `MasrafySelectField` on mobile (010-simple-programs-dbr-calculator)
- PostgreSQL 16 (Prisma migrations only) · Redis 7 (rate limit only; not used by this feature) (010-simple-programs-dbr-calculator)
- Node.js 22 LTS + TypeScript 5.6 (`strict`, `noUncheckedIndexedAccess`) · + NestJS 10, Prisma 5, `class-validator`, `@nestjs/swagger`, `decimal.js` (011-surrogate-admin-panel)
- PostgreSQL 16 (Prisma migrations only). Two migrations: `bank_program_value_sources` (011-surrogate-admin-panel)

### Backend (`backend/`) — feature 001-admin-auth-users

- Node.js 22 LTS, TypeScript 5.6+ (`strict`, `noImplicitAny`, `strictNullChecks`, `noUncheckedIndexedAccess`)
- NestJS 10, Prisma 5, PostgreSQL 16
- `@nestjs/jwt`, `@nestjs/passport`, `passport-jwt`, `bcrypt` (cost 12)
- `@nestjs/throttler` (Redis store), `ioredis`
- `class-validator`, `class-transformer`
- `nestjs-pino` + `pino` (structured JSON logs, PII redact)
- `@nestjs/swagger` (OpenAPI at `/api/docs`)
- `zod` (env validation, fail-fast at boot)
- No constitutional testing requirements (Principle XVI placeholder post-v1.2.0).

### Frontend (`admin/`) — feature 001-admin-auth-users

- Angular 18, TypeScript 5.4+ (same strictness profile)
- Standalone components only — no NgModules in new code
- Signals over RxJS for state
- New control flow `@if` / `@for (... ; track ...)` / `@switch` / `@defer`
- `inject()` DI (no constructor DI)
- Reactive Forms with typed controls
- ng-zorro-antd 18 (`nz-*`) — the ONE UI library, repo-wide. Angular Material is NOT installed; a `mat-*` component in a PR is a review block. Modals/drawers via `NzModalService` / `NzDrawerService` (A34)
- `@angular/localize` (Arabic primary, English secondary)
- No constitutional testing requirements (Principle XXVII placeholder post-v1.2.0).

### Infrastructure

- PostgreSQL 16 (Prisma migrations only — `db push` forbidden in prod)
- Redis 7 (rate limit, lockout sliding-window counters, future refresh-token revoke broadcasts)
- S3-compatible object storage (future feature)
- Docker multi-stage builds, non-root user

## Project Structure

```text
masrafy01/
├── backend/                # NestJS service
│   ├── src/
│   │   ├── auth/           # login, JWT, refresh, logout, me, password
│   │   ├── users/          # admin-users CRUD
│   │   ├── audit/          # AuditEvent writer + queries
│   │   ├── common/         # filters, guards, decorators, error codes, pagination, pino config
│   │   ├── infra/          # PrismaService, RedisService, HibpClient, env schema
│   │   ├── health/         # /health/live, /health/ready
│   │   └── main.ts
│   ├── prisma/             # schema.prisma, migrations/, seed.ts (idempotent)
│   └── test/               # unit / integration / e2e
├── admin/                  # Angular dashboard
│   ├── src/
│   │   ├── app/
│   │   │   ├── core/       # AuthService, ErrorCodeService, interceptors, guards
│   │   │   ├── features/   # auth/, users/, shell/
│   │   │   └── app.routes.ts
│   │   ├── i18n/           # messages.{ar-EG,en-US}.xlf, error-codes.{ar-EG,en-US}.json
│   │   └── styles/         # _tokens.scss (#0869C3 base), _material-theme.scss
│   └── tests/              # unit (Vitest) / e2e (Playwright + axe-core)
├── docker/                 # compose.dev.yml, compose.test.yml
├── specs/001-admin-auth-users/   # spec.md, plan.md, research.md, data-model.md, contracts/, quickstart.md
└── .specify/               # constitution, scripts, templates
```

## Commands (development)

```bash
# Bring up infra
docker compose -f docker/compose.dev.yml up -d postgres redis

# Backend
cd backend
nvm use
npm install
npx prisma migrate dev
npx prisma db seed                  # idempotent super_admin
npm run start:dev                   # http://localhost:3000

# Admin
cd admin
npm install
npm start                           # http://localhost:5173
```

## Constitution Principles — Daily Rules

Tags map to constitution sections. Cite principle # to block PRs.

### Cross-Platform (NON-NEGOTIABLE)

- **I — Money is Decimal**: `Decimal` types only; floats forbidden. `BankOffer` immutable post-creation.
- **II — Banks Are Data**: no `if (programId === ...)` branches; tier resolution generic.
- **III — Typed Errors**: API returns `{ success: false, code: "CODE", meta? }`; no English to clients. Same-PR rule: backend `error-codes.ts` + Angular `error-codes.{ar-EG,en-US}.json` + Flutter ARB.
- **IV — Arabic-First**: all user text via `@angular/localize`; logical CSS only (`margin-inline-start`, never `margin-left`); test LTR + RTL on UI PRs.
- **V — Matching Engine Is IP** (v8.0.0; global pool v10.0.0; asked-weight denominator v13.0.0; all types scoreable v14.0.0): pure module. Formula + tiers in code (PR-reviewed). **Two-level weighted**, normalised over the questions the applicant was ASKED: `probability = Σ_answered(questionWeight × answerScore÷100) ÷ Σ_asked(questionWeight)` (clamped 0..1; per program the ASSIGNED question weights sum to **100** and each answer score is **0–100**). *Asked* = active + assigned to the applicant's category + visible after branching (**no type filter — every type scores, v14.0.0**). Per type, one answer → one 0–100 score, all admin DATA in the same `weights` blob: `SINGLE_SELECT` → the picked option's score · `MULTI_SELECT` → picked scores combined by the program's `aggregation` (`AVERAGE` default / `SUM_CAPPED` / `MAX` / `MIN`) · `NUMERIC` → the score of the half-open `[from, to)` **band** the value falls in (ordered, gapless, −∞…+∞, Decimal edges) · `TEXT` → **presence only** (`answeredScore`; keyword/regex scoring is forbidden). A weighted question with no rule for its type is rejected on save (`WEIGHTS_MISSING_RULE`) — it would eat denominator weight it can never earn back. Weight on a question the category never asks leaves BOTH sides (so it can no longer cap a program forever); an asked-but-skipped question keeps its weight in the denominator and earns nothing. Nothing asked → 0, never `NaN`. No ACTIVE set → `usedDefault`, persisted on the offer and rendered "Not rated", NOT `very_low`. Questions + answer options are **pure content** (label + order — NO scoring/eligibility fields) and **carry NO `category` column — one GLOBAL question pool** with a single versioned snapshot. (v12.0.0) Each question is **assigned to one or more loan categories** via the `question_loan_category` join table (many-to-many — one question can serve several), so the chosen category filters BOTH which *programs* match and which of the pool's *questions* are asked — never which *questionnaire* is served (there is one). Assignment is authoritative (assigned to nothing = asked by nobody), lives only in the join table, and is **frozen into the snapshot** (a pre-v12 snapshot carries none → reads as all categories). `GET /v1/questionnaire?category=` narrows the snapshot (unknown value → `VALIDATION_FAILED`, never a silent fallback) and apply scopes required-question enforcement to the same set. Per bank program, the admin **selects which questions it scores on** (checkbox in the per-program editor) — assignment IS the `questionWeights` key set (no join table) — plus the weights + answer scores, all admin-editable DATA stored in `ScoringWeightSet.weights` as `{ questionWeights: {questionCode→weight}, answerScores: {questionCode→optionCode→score} }` (legacy single-level rows upgrade on read). **Direct admin save** (no maker-checker; editor ID audited; atomic archive+activate). No ACTIVE set (no assigned questions) → score 0. **Eligibility gating dropped for MVP** (no salary/age/DBR/loan-amount/max-loan): every active program in the chosen category returned ranked by probability, in BOTH preview and apply (apply runs the engine with `skipEligibility`).
- **VI — PII Protection**: encrypt at rest; logs never carry PII; documents in S3 with presigned URLs; audit log append-only.
- **VII — Observability**: `X-Correlation-Id` everywhere; `/health/live` + `/health/ready`; structured JSON logs (Pino); discrete business events.
- **VIII — Brand**: `#0869C3` azure blue primary. Use tokens, never raw hex.

### Backend

- **IX — Feature Modules**: organize by domain (`auth/`, `users/`, etc.). `common/` MUST NOT import from features.
- **X — Repository Pattern**: services NEVER touch Prisma directly. Use `*.repository.ts`.
- **XI — Prisma Migrate Only**: `db push` forbidden in prod. Named migrations; indexes on FKs + hot WHERE/ORDER BY.
- **XII — DTO vs Entity**: `class-validator` DTOs; Prisma types stay in repositories. Global `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true })`.
- **XIII — Dual Auth (v3.0.0)**: JWT-only across both API surfaces — HMAC removed platform-wide. Admin JWT (15 min access + 7-day refresh httpOnly cookie). **Customer JWT** on `/api/v1/*` — 15 min access + 30-day refresh, response-body strings (not cookies), stored ONLY in `flutter_secure_storage`. Server-side refresh rotation + reuse detection: replaying a rotated refresh token revokes the session family. Separate signing keys `CUSTOMER_JWT_ACCESS_SECRET` / `CUSTOMER_JWT_REFRESH_SECRET`; bcrypt cost ≥ 12 with `select: false`. Two registration paths, both LITE at OTP/provider then completed via the mandatory profile-completion step (Principle XXXVII): PHONE-signup (mobile+OTP → lite row → firstName+lastName+birthday+password) and SOCIAL (provider → lite row, no password → mobile+OTP+firstName+lastName+birthday). **SOCIAL = Google only (v11.0.0)** — Apple sign-in removed platform-wide (no `/auth/social/apple`, no `/auth/apple/login`, no `APPLE_BUNDLE_ID`, no `sign_in_with_apple`; `SocialProvider` = `GOOGLE`). A second provider needs an amendment. Profile photo + National ID are optional, uploadable any time, never gating completeness (v9.0.0); only National ID is required at the select-offer commitment point (v9.1.0 — profile photo dropped from the gate, optional everywhere). NO guest mode; `Application.isGuest`, `mobileClientId`, and the claim endpoint are removed from code and MUST NOT be reintroduced. Login lockout: 10 failures / 15 min → 30 min. Forgot-password PHONE-only.
- **XIV — API Contract**: envelope `{ success, data, pagination? }`; versioned (`/api/v1/`, `/api/admin/`); OpenAPI at `/api/docs`.
- **XV — Rate Limiting**: `@nestjs/throttler` with Redis backing.
- **XVI — Placeholder**: no constitutional testing requirements (v1.2.0).

### Angular

- **XVII — Standalone Only**: no `NgModule` in new code.
- **XVIII — Signals**: `BehaviorSubject` for component state = review block.
- **XIX — New Control Flow**: `@if`/`@for ... track`/`@switch`/`@defer`; old `*ng*` directives = review block.
- **XX — `inject()`**: constructor DI = review block.
- **XXI — No `any`**: use `unknown` + narrowing.
- **XXII — Typed Reactive Forms**: template-driven forms = review block.
- **XXIII — UI UX Skill Pipeline**: invoke `ui-ux-pro-max` (promax) BEFORE designing every new dashboard screen + invoke `impec` AFTER first implementation for polish/audit. Both mandatory.
- **XXIV — Design Tokens**: theme in CSS custom props; raw hex / raw pixel values outside `_tokens.scss` = review block.
- **XXV — Lazy + Functional Guards**: `canActivateFn`/`canMatchFn` only.
- **XXVI — HTTP Discipline**: `HttpClient` only (no `fetch()`); interceptors for auth / error / correlation / toast.
- **XXVII — Placeholder**: no constitutional testing requirements (v1.2.0).

### Flutter (Mobile — v3.0.0)

- **XXVIII — Clean Architecture + Cubit/Freezed (skeleton)**: data/domain/presentation per feature; customer JWT access + refresh tokens only in `flutter_secure_storage` (no HMAC, no shared-secret); Dio bearer interceptor + silent refresh on 401; `auto_route` v9+; design tokens in `MasrafyColorTheme` (`#0869C3` azure).
- **XXX — Three-Layer Feature Architecture**: `masrafy-app/lib/features/<name>/{data,domain,presentation}`. Models stay in data; entities returned by repositories; `Either<Failure, T>` from every repo method.
- **XXXI — Cubit + Freezed State Management**: one cubit per screen by default; multi-field forms use `updateField(FieldEnum, Object)` with exhaustive switch; cubits are orchestration-only (data logic on the Freezed state); cross-feature cubit sharing forbidden.
- **XXXII — Per-Flow Page Library Pattern**: `presentation/pages/<flow>/<flow>.imports.dart` owns flow's imports; screen files are `part of` it; flow-local widgets under `<flow>/widgets/`; feature-shared widgets under `pages/widgets/`; one widget per file.
- **XXXIII — Shared Widget Reuse**: `masrafy-app/lib/core/widgets/<category>/` is the only home for cross-feature widgets; sheet/dialog/picker/app-bar surfaces extend their mandated base; naming `Masrafy[Action][ModalKind][Sheet|Dialog]`. **Value selection (single/multi) uses the shared instant tap-to-select bottom sheet** (`MasrafySelectField<T>` trigger → `showMasrafySingleSelectSheet`/`showMasrafyMultiSelectSheet`, one `MasrafySelectOption<T>`) — NO inline/floating/overlay/accordion or native dropdowns; kebab/filter/period menus exempt (v8.1.0, A36).
- **XXXIV — Shape-Matched Shimmer**: every async screen renders a shimmer skeleton mirroring the layout; centered spinner on first-load of content-bearing screens = review block; shimmer re-fires on every reload, not only first load.
- **XXXV — Cross-Feature Sub-Feature Reuse**: cubit + state + widgets shared across ≥2 features lives at `masrafy-app/lib/core/features/<concern>/`; promote on second use; each consumer gets a fresh `getIt<>()` cubit.
- **XXXVI — One Screen, One File (v3.1.0)**: every navigable screen ships as exactly one public widget in its own `*_page.dart` (or `_dialog.dart` / `_sheet.dart` / `_picker.dart`) file. Private `_`-prefixed leaf helpers used by only that screen MAY co-exist below the page class. Helpers reused by ≥2 screens → promote per XXXIII. Page files are UI-only (no datasource calls, no token signing).

### Cross-Platform (added v4.0.0)

- **XXXVII — Mandatory Profile Completeness (v4.0.0, narrowed v9.0.0, NON-NEGOTIABLE)**: account unusable (questionnaire + matching + apply gated, backend `PROFILE_INCOMPLETE`) until complete: mobile+verified, firstName, lastName, birthday (PHONE also passwordHash). Profile photo (`profilePhotoKey`) and National ID front+back are OPTIONAL for completeness — never required to complete the profile or reach any gated surface; upload any time via the existing endpoints. National ID (front+back) IS required at the **select-offer commitment point** (non-XXXVII gate, v9.1.0) — 409 `NATIONAL_ID_REQUIRED`; profile photo is NOT gated there (v9.1.0, dropped `PROFILE_PHOTO_REQUIRED` throw); `POST /v1/apply` requires no documents; the mobile docs screen pre-checks via `GET /v1/profile/documents/status`. Age DERIVED from `birthday`, never stored. Photos/ID, when uploaded, follow Principle VI.

## Anti-Patterns (Binding — see constitution Appendix)

- **A1** Hardcoded bank logic
- **A2** English errors to clients
- **A3** Float for money
- **A4** PII in logs
- **A5** Direct Prisma in services
- **A6** Mutable BankOffer after match
- **A7** Schema via `db push`
- **A8** Leaking Prisma types through controllers
- **A9** Reserved — was "Skipping HMAC in mobile tests", retired v3.0.0 (HMAC removed)
- **A10** NgModules in new Angular code
- **A11** `BehaviorSubject` for component state
- **A12** Old `*ng*` control flow
- **A13** Missing `track` in `@for`
- **A14** Constructor DI in Angular
- **A15** `any` type
- **A16** Template-driven forms
- **A17** Skipping the UI UX skill pipeline (`ui-ux-pro-max` + `impec`)
- **A18** Raw hex outside theme
- **A19** `margin-left`/`-right` in stylesheets
- **A20** Hardcoded user-visible strings
- **A21** Manual `fetch()` in Angular
- **A22** Per-component error message mapping
- **A23** JWT access/refresh token outside `flutter_secure_storage` (restated v3.0.0)
- **A24** Approval probability without documented weights
- **A25** Half-updated dependents (cross-surface drift) — Principle XXIX
- **A26** FIFTH retail loan category without amendment / ghost rows after removal / **re-introducing no-payslip as a category or as a hardcoded capable-category list** (Principle II scope-lock, v1.5.0 → v1.6.0 → v1.7.0 → v15.0.0 added `fast` → v16.0.0 removed it). An amendment must carry: Prisma enum value + migration, every surface's category list (backend `ALL_LOAN_CATEGORIES`, Angular `LOAN_CATEGORIES`, Flutter `LoanCategory`), the questionnaire's question set for it, catalog assignments, a colour token, and labels in every locale — in ONE change (A25)
- **A27** Money / amount input without `MoneyInputDirective` (`appMoneyInput`)
- **A28** Mobile datasource/repository/usecase/cubit method with >2 params NOT promoted to a typed `<Name>Request` DTO (Principle XXX, v1.8.1)
- **A29** Multiple route-level widgets in one page file (Principle XXXVI, v3.1.0)
- **A30** Reserved — was "National ID collected at apply instead of profile completion", retired v9.0.0 (National ID timing no longer gated; Principle XXXVII narrowed)
- **A31** Storing age instead of deriving from `birthday`, **or accepting `age` on any customer request body / assuming a placeholder age** — apply / preview / calculator derive it via `CustomerProfileCompletenessService.getApplicantAge()`; only the ADMIN simulator DTO carries an explicit sample `age` (Principle XXXVII, v4.0.0; extended v11.0.0)
- **A32** Proceeding past an incomplete profile (Principle XXXVII, v4.0.0; narrowed v9.0.0) — profile photo/National ID are no longer completeness fields, must not be added back into this gate
- **A33** Mutating ACTIVE weight set in place / hardcoded question weights or answer scores / hand-typed questionnaire codes / adding scoring or eligibility fields back onto `Question`/`QuestionOption` / reintroducing eligibility gates into preview or apply / probability by any formula other than `Σ_answered(questionWeight × answerScore÷100) ÷ Σ_asked(questionWeight)`, or dividing by a flat 100 instead of the asked-question weight (v13.0.0), or scoring an answer to a question that was not asked, or leaving an asked-but-unanswered question out of the denominator, or letting preview and apply derive the asked set differently / assigned-question weights not summing to 100 / answer score outside 0–100 / **reintroducing a `category` field on `Question`/`QuestionGroup`/`QuestionnaireVersion`, or a per-category questionnaire (a second pool / one snapshot per category) — there is ONE pool and ONE snapshot; per-category question scoping is legal but lives ONLY in `question_loan_category` (v12.0.0), must be frozen into the snapshot (never re-derived at read time), and a category-filtered read must fail loudly on an unknown category instead of returning the whole pool** (Principle V, v6.0.0; two-level weights v8.0.0; global pool v10.0.0; category assignment v12.0.0)
- **A34** Modal/dialog/sheet backdrop that dims only the content panel, not the full viewport — usually a `position: fixed` scrim trapped inside a containing-block ancestor (`transform`/`filter`/`contain`, e.g. `section.page`'s `app-page-rise`). Use `NzModalService`/`NzDrawerService` or render the scrim as a root-level sibling; shared backdrop tokens (Angular Clean Code Structure, v4.1.1)
- **A35** Gradient hero with a hardcoded `height`/`expandedHeight` literal that clips content (use content-sized `MasrafyGradientHeader.expandedHeightFor(...)` → sliver `expandedHeight` + static `heightInPixels`, ~180px `minHeight` floor), a collapsed toolbar that ellipsis-truncates the title (use `FittedBox(scaleDown)`), a hero hugging content with no breathing space, or a static (non-collapsing) hero on a scrollable screen. Scrollable screens host the hero in `MasrafySliverGradientHeaderDelegate` (`SliverPersistentHeader(pinned)` + `CustomScrollView` w/ `BouncingScrollPhysics`), collapsing to a compact toolbar (`heading4` title centred on the back-button row, subtitle faded); a button-driven `PageView` wizard keeps the static header ONLY for non-scrolling steps — a step whose form scrolls hosts its OWN per-step collapsing sliver hero (each `PageView` child = its own `CustomScrollView` + `SliverPersistentHeader(pinned)`; a single `NestedScrollView` over the PageView is forbidden — shared offset pre-collapses short steps), the `bottom` progress bar fading with the hero on collapse (Principle XXXIII, v5.1.0; clarified v5.1.1, v5.1.2)
- **A36** Dropdown / floating-overlay / accordion / native `DropdownButton`/`DropdownMenu` / value-picking `PopupMenuButton` used for form-field value selection instead of the shared instant tap-to-select bottom sheet (`MasrafySelectField<T>` trigger + `showMasrafySingleSelectSheet`/`showMasrafyMultiSelectSheet`, one `MasrafySelectOption<T>`; row tap applies + closes, long lists `showSearch: true`); caller-owned `openField`/`toggleField` accordion plumbing for selects forbidden; kebab/filter/period menus exempt (Principle XXXIII, v8.1.0)

## Recent Changes
- 2026-09-03 (v23.0.0): MAJOR — **what a no-payslip product ASKS is picked from the question pool, per loan type, and membership becomes a table.** Step ① of `/program-catalog/products/:key` was a read-only list of what the blueprint seed created; the operator asked for the tick-grid the catalog name's step ③ already has, and — their words — "you also must ask admin to select the loan type like you already did in normal income proof programs". So: the four loan-type tabs above the grid, and ticking a question inside one makes this product read that answer AND makes that loan type ask the question. **`platform_enumeration.surrogateProductKey` cannot carry this, and the trap is measured, not theoretical.** That column is single-valued and means AUTHORSHIP ("provenance, never a constraint"); the planner withholds it for a fact several blueprints share (a cascade would kill one product's axis when another went away) and for every cap-only blueprint, which mints no product row for a fact to be filed under. On the live database that was **12 of 27** facts filed, so `club_branch_cap` — a product whose entire content is one ask — rendered "asks the applicant nothing yet", and `school_type`, read by three blueprints, appeared on nobody's list. A live defect, not a new one. **New join table `surrogate_product_ask`** (`productId`/`factId` → `platform_enumeration.id`, real FKs on both ends because both address the primary key, `ON DELETE CASCADE` so an allowed fact delete cannot leave the ghost rows A26 forbids, and a STORED `source: blueprint | operator`, which is load-bearing twice — the seed must re-assert its own asks without claiming an operator made them, and the untick refusal reads it). Migration `20260903090000` backfills what the column does hold, joined BY TYPE ON BOTH SIDES (the same column on a `program_name` means something else entirely, so a key-only join would file catalog names as facts) and RAISEs on a mistyped join or a dangling product — deliberately NOT on "a fact is asked by no product", which is the majority state on every real database and would abort a healthy deploy. The other half needs `BLUEPRINTS`, so it lands in `npm run seed:blueprints`: a separate pass, not a plan step, because a cap product's row is minted AFTER `createFromBlueprint` returns and because only a pass can also cover the `skip` action — a product that already holds a calculation still needs its list rendered. Insert-only, so it can never undo an operator's pick; measured at 10 rows recorded, then **0 written and 0 questionnaire versions cut** on the second and third runs. **Three routes on the product's own controller** (declared before `:programCode` — the trap that file documents): `GET …/asks` returns the whole board, `PUT …/asks/:questionCode` attaches, `DELETE …/asks/:factKey` detaches. Addressing is asymmetric on purpose — attach names the QUESTION (what the operator picked; the fact may not exist yet), detach names the FACT (what exists, and what the screen renders). ONE composite call per click, and the read is new rather than three existing reads composed because they cannot be: the question pool sits behind a stricter role, the fact registry reaches the admin through a per-session cache a tick cannot invalidate, and "which other products read this fact" is answerable only from the new table. **REUSE-FIRST is the rule the whole design turns on**: a question already answered by a fact JOINS that fact and mints nothing — verified on `school_type`, where a tick added an ask row, left `surrogateProductKey` NULL, produced no second key and left the other two products reading it. A rival key would put two bank tables under two names for one answer with nothing on screen saying which a rule should read. Otherwise the key is the question's own `code`, verbatim (already an immutable slug of the same wording, so a rewording cannot move it, and staging and production mint the same key); an unbound row of that key is BOUND rather than refused forever; **never `uniqueSlug`** — a `_2` suffix is a permanent second name for one answer. **Eight new error codes** (227 in sync across backend and both dictionaries), and the refusals that matter are the ones nothing else catches: `monthly_income` and the three other money-field bindings (binding the declared payslip would let a no-payslip rule read the very figure a withheld product rule exists to keep out of a quote), the itemised debts **except** `credit_card_total_limit` (which already IS the platform fact `credit_card_limit`), the bank axes, and a **required** question being widened into a loan type that does not ask it yet — apply reads live assignments while the customer is served a frozen snapshot, so that would refuse every application in that loan type until the publish lands, permanently if it fails. `TEXT`/`MULTI_SELECT` are listed and non-tickable with the reason on the card; a parked question is refused as parked and a code naming no question at all as `QUESTION_NOT_FOUND`, because "switch it back on" is only true of the first. **Categories are written ADDITIVELY** — new `addCategories`/`addQuestionCategories` using `createMany({skipDuplicates})`, because the existing setters replace the whole set and a four-tab screen is exactly where two whole-set writes interleave and one tick vanishes with the losing UI still showing it ticked — and the questionnaire is published **once per click, only if a loan type actually moved**: measured 142 → 143 on a tick that widened, and 143 → 143 on the re-tick. The customer-facing proof is the frozen snapshot: `loan_purpose` appeared in the ACTIVE version's own `categories` after the click. **Untick detaches only**: the ask row goes, the question and its loan types are untouched (asserted as an absence, and the fact row survives unless nothing is left of it — no other product asks it, nothing anywhere reads it, this product authored it, and it is not the platform's). Verified as three outcomes on a real database: a shared fact kept its row with the other two products still reading it, a platform fact kept its row, and the product's own fact was deleted with the question's loan types unchanged and no version cut. **`countReferences` for `surrogate_fact` was decoration and is now the guard it claimed to be.** It matched `incomeAssumption.strategy === 'fact:<key>'` and nothing else — but every predefined product compiles to a PIPELINE, whose strategy is the constant `'steps'`, so for every product this platform sells the count was zero (CLAUDE.md has carried this as "Found, not fixed" since v18.4.0, by which point three surfaces were uncounted). New pure `fact-readers.ts` unions all four — the strategy token, `steps[]`/`gates[]` via the validator's own `factsReadBy`, the per-bank `additionalIncome` sources, and `loanLimits.maxLoanByFact.{factKey,columnFactKey}` plus `maxLoanAdjustments[].whenFactKey`, which is the ENTIRE reader set of a cap-only product — and both the untick and the generic `DELETE admin/enumerations/:id` route through it. Proved over HTTP: a cap table whose COLUMN read a fact refused both the untick and the generic delete, naming `bank_program_cap`, and the program's blob was restored byte-identical afterwards. Two scans and a pure walk rather than JSON path filters, because Postgres cannot index into a JSON array by path — which is precisely how the original single-token count came to miss every pipeline. `valueSources` is deliberately not a surface (its paths are step-id and option-code keyed, so a detach dangles no marker; retiring an OPTION does, which is a different change) and neither is `templateSpec` (compiled into `incomeRule` in one statement, so scanning both would be two answers to one question). **The v22.0.0 door is reopened by exactly one seam and no more**: `CreateEnumerationOptions.source` gains `'product_ask'`, still THIRD and POSITIONAL so no wire field can reach it, and a per-source allow-list lets it create a `surrogate_fact` and **not** a `surrogate_product` — an operator picks which seeded product this platform sells and what one of them reads, never mints a product. Pinned by a test that also re-asserts the generic `POST admin/enumerations` and the bulk-paste door still refuse both kinds. **Admin: three sections, not two**, because the tri-state is real and each needs a different verb — *read and asked here* (tap to detach), *read but not asked of X applicants* (an `<li>`, not a checkbox: it offers **Ask it here** and **Stop reading this**, and a link nested inside a `role="checkbox"` button is invalid markup a screen reader reads as one thing), and *not read yet* (tap to attach and widen). Attachment is GLOBAL and only the section, the counts and what a tick would widen move with the tab — the one thing a reader of a tabbed grid assumes wrong, so it is pinned by a test. New pure `product-asks.ts` (+24 specs) owns every derivation; the page keeps an in-flight OVERLAY rather than a patched copy of the board, because the truth lives in a response it does not own — there is no second copy to roll back wrong. `refresh('surrogate_fact')`, never `load()`, after a write (the v19.0.0 bug), and a re-read with `keepEdits` because a new fact can bring a list on stage below the grid. Step ①'s rail status can now read `invalid`, which is a strict improvement: a product whose only fact is asked in NO loan type used to read `done` and could never quote. **`confirmOff()` corrected in the same change**: it derived affected fact keys from the ownership column while the server flips the ones the BLUEPRINT declares. It happened to UNDER-state; once an operator can attach a fact of their own it would start OVER-stating, promising that answers stop being read which the server will not touch. It states the consequence in words now and reports the `factsChanged` the response already carried and the client discarded. **`impec` measured rather than eyeballed, and the first harness was wrong**: a regex over `getComputedStyle` returned 1.36:1 on a 600-weight label, which is impossible — colours are now resolved through a canvas, and every element passes AA in both themes (blocked reason 5.29 / 7.56, tag chip 5.29 / 7.56, section heading 6.17 / 8.91, type label 5.74 / 7.95, question label 14.04 / 16.02). Fixed by that pass: the blueprint refusal repeated verbatim on all seven cards (three identical lines per card, which stops being read at the second one) → a two-word chip plus one note for the section; the house `.tag` at tertiary ink with no chip → a real chip at secondary; a ticked-and-fixed card losing its READ tint → the dashed edge says fixed and the tint says read; and the missing fourth state (`:active`). **`--color-text-inverse` is defined by no palette in this theme** — a `var()` with no fallback is invalid at computed-value time, so the check glyph would have inherited the body ink and sat dark-on-azure; it reads `--text-inverse`, the token that exists. The blocked cards carry `aria-disabled` and never the `disabled` attribute, verified by keyboard: 2px solid focus ring on `:focus-visible`, a blocked card still reachable by Tab, and its `aria-describedby` resolving to the reason. **The Arabic bundle caught a defect nothing else could**: nine of my targets carried the literal text `{$INTERPOLATION}` instead of the `<x/>` placeholder element the source declares — legal XLIFF that silently DROPS the value, so the heading read "not asked of applicants" with the loan type missing. Fixed and re-verified on the real `ar-EG` build, which also needed serving locally because its dev server cannot authenticate. 41 new i18n ids with ar targets, 7 orphans dropped, untranslated ids **423 = baseline**. Backend 1339 → 1417 tests, admin 128 → 152, both locales build, `check:codes` / `check:income-proof` / `check:parent-keys` clean, and lint on every touched file at or below baseline (one file improved; two that a bulk lint-fix had reformatted were reverted rather than smuggled in). Browser-driven against a real database in light, dark, forced RTL and the real Arabic build, with page overflow measured at **0** at 1440, 1024 and 720, and no console errors beyond the app's own pre-existing 401 session probe. Every test row was removed afterwards and the one question the run widened was restored to its original loan type. **Not done, stated:** `PRODUCT_ASK_READ_BY_OWN_RULE` and the TEXT type refusal are covered by unit tests but were not exercised over HTTP — the first needs a rule that names an operator-attached fact (a two-step flow through the calculation screen) and the second has no subject, because the pool contains no TEXT question at all; no bulk "tick all listed" (each tick can publish, so a bulk button over 49 questions is a publish storm and a refusal storm behind one click, and doing it properly needs one endpoint taking the whole set); narrowing a question's loan types still lives only on `/questionnaire/categories`; and `promax` was not run before the grid, deliberately — it reuses the catalog name's own vocabulary verbatim, which is a tighter spec than a generated system, and `impec` ran after as Principle XXIII requires.
- 2026-09-03 (v22.1.0): MINOR — **the amount each answer carries is typed beside the answers, on every surrogate product.** `/program-catalog/products/:key` step ① listed "The answers they pick from" — Apartment · Twin or town house · Villa — with no figure anywhere, while the amounts those answers key had been stored, inherited and editable for versions, two steps to the right and named after a STEP rather than after the list. **No new storage and no backend change:** the boxes write `incomeRule.stepParams.<slot>.keyTable[<optionCode>].incomeEGP`, the same store step ② and the calculation screen already write, through the same `app-product-rule-editor variant="catalog"` — a second editor that knew how to name a figure would be a second authority on what it means. What is new is which slots are on screen. **A figure belongs to a SLOT, never to a lookup value**, and that is not a preference: `platform_enumeration` has no numeric column, one list is read by two slots in different UNITS (an EGP ceiling and a percent factor), the same list is shared across products, and one value legitimately carries two figures (a `pickByFact` pair). New pure `admin/src/app/shared/income-rule/figure-slots.ts` (18 specs) states the join once — `factChoiceTable` and a `numberByKey` gate are keyed by the ANSWERS (`question.optionsEnumerationType`), `factParentTable` by the CLASSES (`parentEnumerationType`) — mirroring `groups()`, `columnSlots()` and `gateSlot()` rather than restating them. **The case a naive build gets wrong is silent and looks right**: the compound NAMES list carries no figure of its own (the amount is stated per class), so keying off the answer list prints "No default set" against three hundred compounds and invents a defect where there is none — it reads `byClass` and says where the figures actually are. A list nothing keys says so too, and a cap-only product says each bank states its own maximum. Filtered by SLOT and not by row, because a pick whose two columns read different facts is one row with two list homes. Editor gains two additive inputs (`onlyKeyedBy`, `layout='inline'`): inline drops the rail, the lede, the fact chips and the flow — all of it said by the panel above — shows every surviving group (there is no rail to switch with), opens collapsible rows (a blank optional row folding behind "No default set" is right on a whole calculation and wrong under the list somebody opened to type into), and `narrow()` recomputes `configured`/`state` from the surviving slots so a row cannot report on somebody else's list. **ONE Save covers both steps** (the bar moved to an `ng-template` rendered on ① and ②) and step ① now reads `invalid` while dirty: it is one unsaved object, and two buttons would be two writes racing over one JSON blob. **Two data-loss bugs this opened, closed in the same pass:** `onListChanged()` re-read the product and overwrote the figures, so adding a compound after typing three amounts discarded all three — it now keeps unsaved edits (a value write cannot change the rule's STRUCTURE, only the option list behind it); and it called `PlatformEnumerationsService.load()`, which returns the cache when it has one, so a newly added value never reached the key table's option list until a hard reload — `refresh()`. **The blocker found by driving the screen rather than reading it, and it predates this change:** Save was DEAD on every blueprint-built product. The client mirror `productRuleHasError` was missing BOTH corrections `validateProductRule` already carries — a mixed `coalesce` whose blank step sits beside a `{const}` is never empty (the literal IS the fallback, and `coalesce[iscore_band, {const:'100'}]` is the I-Score shape every seeded product carries), and the check must recurse, since `pickByFact` states no figures of its own and reads as configured unconditionally. The mirror out-refused the server, which is the worst direction: the operator is told their figures are unsavable with nothing to fix. Pinned by `product-rule-blocked.spec.ts`. **Also fixed while adjacent:** the NG0600 that `lookup-value-list.component.ts` has thrown on every screen with a values panel since v20.2.0 recorded it (a signal written from an `effect`, so the row window never reset on a new filter) — twice per product page, which is where it became this change's problem; and the key table's `nz-option` rendered `labelEn` in both locales, an English option list sitting directly beside the Arabic values it is keyed by. **Bank side: one branch, no mechanism change.** Override already works — the catalog's figures are pre-seeded into the bank's grid and the first keystroke keeps the whole set as that bank's copy — and per-slot inheritance is refused deliberately: blank already MEANS "this bank declined this way", so inheritance-by-absence would silently switch a bank onto a derivation it never chose, and a partial `keyTable` is answered by `no_matching_row`, which is fatal. What the two-state strip could not say is the state every product ships in: the name states a proof, the catalog states no figures, the grid renders empty, the wizard's own gate is off for catalog amounts by design, the save succeeds and the resolver then answers every applicant with `rule_unconfigured` — now a third branch with a warning accent, advisory rather than a gate, since the fix is on a screen that operator may not own. **Verified against a real database and a real browser:** `compound_owner`'s stored 2,000,000 / 3,000,000 / 4,000,000 rendering under *Owned unit type* with the yes/no list correctly saying it carries no amount; `compound_owner_ceiling` putting its figures on the CLASS list and the compound names reading `byClass`; and a full round trip on `school_stage_ceiling` — empty table → *Add a row for every key* → 6 boxes (3 stages × 2 columns) → Save DISABLED while any row is blank (the new mirror of the server's `incomeInvalid`) → enabled once filled → 200 → persisted through a reload → restored to empty afterwards. Page overflow measured at 0 in light and dark at 1440/1024/720 and under forced RTL, no page errors. 9 new i18n ids with ar targets, 2 sources reworded with their targets, untranslated ids **423 = baseline**; admin tests 107 → 128, both locales build, lint below HEAD on every touched file bar the repo-wide `*Page` suffix convention. **Found, not fixed:** `compound_owner`'s stored rule reads the fact `which_compound_is_your_unit_in`, which is in no registry (its blueprint twin `compound_owner_ceiling` reads `compound_name`), so EVERY save of that product — including re-posting its own unchanged figures — is refused `INCOME_RULE_FACT_UNAVAILABLE`; the dangling link is data, it predates this work, and the screen now reports it inline instead of silently. **Not done, stated:** the bank-side empty-catalog notice was not exercised in a browser — this database holds zero `income_surrogate` bank programs, so reaching that strip means creating one; and no figure is seeded anywhere, the blueprint prohibition standing exactly as written.
- 2026-09-03 (v22.0.0): MAJOR — **a no-payslip product is SEEDED, not created: the admin's only lifecycle action on one is ON / OFF, and OFF stops it being used anywhere.** v21.0.0 gave the operator a library to BUILD products from; this removes the building. Nothing seeded the eleven, so a database held whatever somebody had clicked — the screenshot that started this showed 2 products where a known library belongs. **New `npm run seed:blueprints`** runs all eleven through the real `BlueprintService.createFromBlueprint`, so a seeded product is byte-identical to one the UI produced — same refusals, same audited writes, same single questionnaire publish. It **cannot be a `tsx` script**, and that is the trap the obvious design walks into: `tsconfig` sets `emitDecoratorMetadata`, `tsx` is esbuild, esbuild does not emit `design:paramtypes`, so `NestFactory` under `tsx` fails to resolve every provider — every existing seed is `tsx`, which is exactly why it is written down. So it lives in `src/` and runs compiled (`tsc` type-checks it, eslint sees it; `scripts/` is in neither glob). It boots a **dedicated `BlueprintSeedModule`, never `AppModule`**: `BootstrapModule`'s `AdminSeederService.onApplicationBootstrap` RESETS the super-admin's password on every boot and `createApplicationContext` runs bootstrap hooks — observed in the log, not inferred. **It never touches a product that already holds a calculation** (`blueprint-seed-plan.ts`, pure, with its own spec: `create` · `resume` · `skip` · `cap`), because an operator may still edit a product's figures and a re-run would undo their work on the next deploy. Measured on a real database: 8 products + 3 cap-only built, then a re-run writing **0** and cutting **0** questionnaire versions (142 → 142). **OFF is an engine change, not a filter.** The read path ignored `active` on purpose, and the naive fix — `where: { active: true }` on the products half of `programNameIncomeRules()` — is **silently wrong**: on a single-fact product (`byGrade`, `byProfessorRank`, i.e. every pre-template one) the missing rule falls through `income-resolver.ts` to "a declared salary still carries the quote", and `monthly_income` is a required question, so the program would keep quoting off a payslip the bank never agreed to lend against and freeze it onto an immutable offer. So `effectiveProgramNameRule` returns a RESOLUTION — `{rule}` or `{withheld:'surrogate_product_retired', productKey, rule?}` — the withheld marker **wins over the name's own grandfathered rule** (switching a product off must not revive a table nobody chose), a DANGLING link still reads as an omission (different cause, pinned by its own test), and the marker still CARRIES the rule so the admin save path merges the catalog's figures exactly as before: switching a product off is a decision about what QUOTES, not about what is configured. New 200-body reason **`SURROGATE_PRODUCT_RETIRED`** (the program stays LISTED and ranked, A33) refused at the TOP of `quoteProgram` — before the pricing cascade and before `input.incomeResolution` can be pre-supplied by a caller — mapped to `program_misconfigured`, never `monthly_income`, so the suggestion engine does not propose a guarantor for a switch only an operator can flip. Apply/preview parity is structural (one `quoteProgram`); the calculator reports it as itself instead of `CALCULATOR_INPUT_INVALID`; `bank_offer` is untouched. **Verified end to end on a real database**: product ON → the program quotes 303,590.83 · OFF → `SURROGATE_PRODUCT_RETIRED` with no figures · back ON → the same 303,590.83. **The retire refusal is inverted**: `SURROGATE_PRODUCT_IN_USE` no longer fires — refusing a switch-off while names are linked would leave a product that can never be switched off, a linked name being the normal state — and the code is **retired but retained** (it has shipped, and the parity check is bidirectional). Four comments across three files asserted "the read path ignores `active` BECAUSE the refusal exists"; all four are now false and all four were rewritten, `assertFallbackUsable`'s borrowed analogy included. **Eleven cards on the operator's explicit call, made safe rather than accepted**: three blueprints (`company_coding_cap`, `school_type_cap`, `club_branch_cap`) guess no income by design and create no product — each bank states the maximum on its own program — so the seed mints their rows and new **`SURROGATE_PRODUCT_CAP_ONLY`** refuses linking a catalog name to one, which is what stops a live no-payslip name that works out nothing. Their OFF acts on the FACT instead, and the first implementation was **inert**: it flipped facts by `surrogateProductKey`, and a cap blueprint creates no product row for its facts to be filed under, so on the real database every one was filed under nothing — measured as `factsChanged: []`, then re-read from the BLUEPRINT with `sharedBlueprintFactKeys` excluded, so turning the school-type CAP off cannot stop the school-stage CEILING reading the same answer. **Create doors closed at the service, not the controller**: `POST admin/enumerations` accepted `surrogate_product` and `surrogate_fact` freely (the shape picker's and the Add-program-name flow's door), now `SEEDED_ONLY_TYPES` + **`ENUMERATION_CREATE_NOT_APPLICABLE`** with a third POSITIONAL `{source:'blueprint'}` bypass no wire field can reach. `POST surrogate-products/from-blueprint` and `DELETE surrogate-products/:key` are deleted, with `deleteSurrogateProductCascade` from all three repositories; new **`PUT surrogate-products/:key/active`** delegates to the enumerations admin service so the audit event, the cache invalidation and the remaining guards stay one implementation, and never sends `deprecate` (`updateById` clears `deprecatedAt` on nothing, so it would be one-way — checked against the real rows first, none deprecated, so no un-deprecation was added speculatively). `purge:handbuilt` defines hand-built as `templateSpec IS NULL`, which is now the cap products' PERMANENT state — it would have deleted three seeded products and their questions, so it skips them. **Admin: five files deleted** (the library, the shape picker, the shape-card component, the by-hand "add an ask", and the library's pure module), the on/off control added in two places that cannot drift (the board's product card, and the slot the product page's Delete vacated), the Add-program-name flow collapsed from two writes to ONE with the product-minting branch, its recovery signal and its `?from=`/`?then=` plumbing gone, and the asks list made READ-ONLY — removing one deleted the question outright, which with the add screen gone was a one-way door on a product an operator cannot recreate. The confirmation STATES the consequence against the program codes already on screen rather than asking "are you sure"; ON never confirms. **The board's off-in-use warn tag is the state the merge could hide**: `catalog-board.ts` joins names to a product regardless of its active flag, so a switched-off product's names sit inside a muted card — live names quoting nothing on a board that reads as complete. **`impec` measured rather than eyeballed, and both findings were load-bearing for this change**: the `.tag` that now carries the OFF state was `--color-text-tertiary` on `--color-surface-muted` = **3.28:1** in light (4.44:1 dark, which is why a light-only failure survives a dark review) → secondary, 5.29:1; and `.tag.warn` was `--color-warning` on its own 14% wash = **2.53:1** → primary ink with the warning carried by a dot and the wash (14.35:1 light, 13.4:1 dark), which is the fix the deleted library page held and this change would otherwise have deleted. **Found by driving a browser, not by reading**: deleting the `products/shapes` route does NOT fall through to `**` — it matches `products/:key` with the key "shapes" and renders a product page for a product nobody has, so it redirects like its sibling. 3 new error codes (219 in sync), 25 new i18n ids with ar targets, 3 sources reworded with their targets, 165 orphans dropped — untranslated ids **423, one under the 424 baseline**. Backend 1316 → 1339 green, admin 107 green, lint at or one under baseline on both sides (backend 111 = 111, admin 94 → 93), both locales build. **Verified in a browser against a real database**: 13 product cards, the confirm naming the right counts, OFF → muted + both tags → surviving a reload → ON with no dialog and the muted class cleared, the DB agreeing at every step and every product left switched on; 11 routes each with overflow 0 and exactly one sidebar item lit, in light, dark and RTL at 1440/1024/720, with no page errors beyond the app's own pre-existing 401 session probe. **Not done, stated:** the ar bundle's own dev server cannot authenticate (CORS allows :5173 only), so RTL was measured with `dir=rtl` forced on the en bundle and the translation half by the ar BUILD (0 of the new ids untranslated, no source mismatches); the lookup drawer's product select filters by `active` but cannot exclude a cap-only product (its rows carry no `outputKind`, and reaching into a feature service from `shared/` would cross the boundary the repo has been cleaning up) — the server's typed refusal covers that path; a product's QUESTIONS keep being asked while it is off, because deactivating one republishes the questionnaire and cannot un-ask an answered question; and the two legacy rows `armed_forces_grade` and `compound_owner` survive beside their blueprint-keyed twins, since the seed adopts nothing it did not create.
- 2026-09-03 (v21.0.0): MAJOR — **a no-payslip product is PICKED from a library of real products, not assembled from a shape; picking one builds everything it asks; and the raw step builder is deleted.** `/program-catalog/products/new` offered eight SHAPES — "a table by a choice answer", "a ceiling from an amount bracket" — correct and anonymous. An operator picking one still had to know that the doctors' product bands years in practice, that the brackets are 3–5 / 5–8 / 8–11, that the second column is a city tier and not a bank relationship, and that the tier is read through the CLASS a governorate is filed under; then author the question, the list and the fact by hand on two more screens, in the only order the foreign keys allow. `docs/surrogate-income-templates-implementation-spec.md` already carried all of it — mechanism, rows, columns, conditions, adjustment scopes, caps and each bank's published figures — and none of it was in the product. **Eleven `ProductBlueprint`s** (`backend/src/bank-programs/blueprints/`) now name a PRODUCT and everything it needs: armed-forces grades · academic rank × university type · years in practice × governorate tier · a share of a card limit · the lower of a multiple of a car instalment and a share of the auto loan · a share of pledged savings · the compound guarantee (four ways, `combine: lower`) · school stage × school type · and three that guess no income at all and only cap the loan (employer coding, school type, club branch). **One card per PRODUCT, never per sheet:** four banks sell the compound guarantee off four different derivations, and that is one product with four ways — each bank fills the ones it sells — where four cards would be the same product duplicated and the fifth bank would mean a fifth card. `POST admin/bank-programs/surrogate-products/from-blueprint` writes the lists (+ their class axes), the values, the questions (options MIRRORED off the list, so an option code IS a registry key), the `surrogate_fact` rows and their bindings, the loan-category assignments and the compiled calculation — then **ONE** questionnaire publish. **Idempotent by key, always**, and that is not a nicety: `uniqueSlug` mints `compound_2`, keys are immutable, and a duplicated list then appears in every bank's stored configuration forever (§10.9). Re-running every blueprint writes nothing and publishes nothing — measured. **`GET …/product-blueprints`** reports, per ask, whether the question / fact / list is already there and **which loan categories it is missing** — the half of "is this set up?" no other screen can show, because a question can exist, be assigned to no category, and be asked of nobody while every list screen reads it as done. **Three engine additions, all FIELDS on existing ops, no new op:** `ProductTemplate.share` (a percentage of the figure for one answer — the joint-ownership halving two sheets state, which had no representation and could not have one, since turning `uplift` into an array would renumber its slot ids); `pickByFact.branchOn: 'parentClass'` and `maxLoanByFact.rowVia`/`columnVia` (a column or a cap row keyed by the CLASS an answer is filed under — one bank tiers Cairo & Alexandria against everywhere else, another tiers eight governorates against everywhere else, and neither "everywhere else" is the other's, so the platform's answer is 27 governorates under three tiers and a column matching option codes could not read that list at all). Every one is absent-by-default and fenced at save (`branch_on_not_applicable`, `via_not_applicable`, `share_same_option`) — a flag on an op that reads no branch list, or a class-keyed axis on a numeric or a DERIVED fact, would not fail: it would match nothing and quietly read the standard column for every applicant. **The uplift's `scope` reaches an operator for the first time** (two radios, the consequence written out): the engine has honoured it since v20.x and the form never asked, so every UI-authored uplift compiled as `income` — the §10.4 question worth 300,000 on one applicant, left to a default. **The figures screen becomes the product's own:** with a `blueprintKey` it opens on *What are the numbers?* rather than on three questions the library already answered, states that product's mechanism and — in grey, never saved — what a published sheet puts in those boxes, and offers the sheet's BRACKETS to any band table it belongs in. The slot each bracket list belongs to is resolved SERVER-side by `waySlot`, because that function is the authority on where a bank's figures live and a slug worked out in the browser would be a second statement of it. The figures stay blank: `validateBands` refuses a band with no figure, so edges alone cannot be persisted — which is exactly the guarantee that no bank's number is ever written by this feature. **Removed:** `product-rule-builder.component.ts` and the one-way reveal it hid behind. A calculation is shaped by the library or by the form; a product that predates both keeps quoting and the screen says plainly that there is no form for it, offering a rebuild from the library rather than a graph to edit. **Found by exercising it against a real database, not by reading it:** the planner reused SOFT-DELETED questions (a purge cannot hard-delete one — `application_answer` is RESTRICT — so a dead question keeps its immutable code), the facts bound to them served nothing, and the save was refused naming a FACT nobody could see was fine → they are REVIVED instead, and four were; fact existence was read from the USABLE registry, which filters to bound facts, so an unbound fact read as absent and the create hit its own duplicate key with no way forward → it is bound instead; a fact two blueprints read was filed under one product and died with it on delete → a shared fact belongs to the platform; and creating seven values one at a time cut SEVEN questionnaire versions, because a value write to a mirrored list republishes — closed by batching through `createValuesBulk`, which needed an explicit per-row `key` (the bulk path slugs from the English label, and `slugify('Major General')` is not `grade_major_general`) and a `deferMirrorSync` for the in-process caller that touches three lists and publishes once. **`--primary-visible` was defined in the DARK palette only**, and four components reach for it: in light mode `var()` was invalid at computed-value time, so a `color` fell back to the inherited ink and nobody noticed while an SVG `stroke` resolved to `none` and every medallion rendered as an empty box — measured, then defined in light, which fixes the three files that predate this one. **`impec` after, with the contrast measured rather than eyeballed:** `--text-tertiary` is 3.83:1 on a card and 3.54:1 on a subtle panel in LIGHT mode (it passes at 5.24:1 in dark, which is why a light-only failure survives a dark-mode review), so the worked example, the ON-A-REAL-SHEET label, the `+N more` chip and the editor's own micro-label moved to secondary; the warn tag copied the board's existing pattern, which measures **2.80:1** (`--warning` on its own 14% wash) — inked `--text-primary` with the warning carried by a dot and the wash (13.19:1 light, 13.49:1 dark), and the board's own tag has the same problem and predates this; the sheet strip's accent rule went from `--primary-subtle` (invisible on its own ground) to `--primary`; the "reads the answers" chips had `--color-surface-elevated`, which resolves to the panel's OWN ground, so the chips had no chip and the row read as one run-on line; a class-keyed column heading rendered the raw slug `city_tier_major` until the label lookup was taught to consult the class list already on the fact; the column hint said "only if this bank lends more to customers it already has", which is true of a new-customer column and false of a city tier, a school type or a university type; and the generic lede is withheld where the product has its own sentence, since four prose blocks stacked before the first control — one repeating the group heading below it — is a page an operator scrolls past. **Verified end to end against a real database and a real browser:** all eleven blueprints created (11 → the three caps returning a fact key and no product), every one re-run writing nothing, the compound product compiling to 22 slots with `minOf`+`skipUnset` under a `coalesce`, `share` and three gates; the §13 acceptance figures pinned as tests (Major → 30,000 · Dean 100,000 / 300,000 private · 60,000 limit → 30,000 · Mivida → AA → 6,000,000 · the lower of 3,000,000 and 4,000,000 · NTB 1,250,000 / X-SELL 1,750,000 · exactly 5 years → the 5–8 row · Cairo 1,500,000 vs Tanta 500,000 · CAT B → 1,000,000 · both `onNoMatch` directions); the library and the figures screen driven in light and dark with page overflow measured at 0, the medallion at the start edge under RTL and zero page errors beyond the app's own pre-existing session probe. 1 new error code (`PRODUCT_BLUEPRINT_UNKNOWN`, 216 in sync), 84 new i18n ids with ar targets, 5 orphans dropped and 3 retargeted, **untranslated ids 424 = baseline**. Backend 1155 → 1316 green, admin 108 → 127, both locales build, lint below HEAD on both sides bar the repo-wide `*Page` suffix convention. **Not done, stated:** a conditional waiver ("the 18-month rule is waived for a fully settled unit", "X-SELL needs a card of at least 100,000") — a gate applies whenever the bank filled its figure, and there is no "unless"; a cap row holding a formula or a second figure (the CD's "before three months, 10% of it" and the club branches' own down payments); mixed `min`/`max` across ways, so the CDs product ships its 30% way with the floor recorded as unrepresentable; the Arabic COMPOUND percentage, whose four readings are 720,000 apart on one applicant (§10.5) — the product ships, the card says a bank still owes the answer, and no percentage is seeded; and an age-banded column, which that same unresolved formula is the only caller for.
- 2026-09-01 (v20.4.0): MINOR — **a product's ways are picked where the product is created: the shape cards go multi-select, and a ceiling may be keyed by a plain choice.** The engine has carried N ways per product since the template layer shipped — `alternatives` up to `MAX_WAYS`, `combine: 'lower' | 'higher'`, compiled by `emitBasis` into `minOf`/`maxOf` with `skipUnset` wrapped in a `coalesce` — and `product-template-compile.spec.ts` already pinned the exact pair a compound product needs (a table by unit type at 4M against a table by compound class at 6M → 4M). What was missing was the door: the create screen's cards were RADIOS on the reasoning that "a product is worked out ONE way", so every product was born one-way and the second way lived behind a checkbox in *Does anything else apply?* on the next screen. Two gaps closed. **(1) `ceiling_by_choice`, the eighth starter** (`maxAmount` + `choiceTable`). "Villa lends more than an apartment" is a choice answer producing a loan amount, and no card offered it — the only choice-keyed card was `income_by_rank`, an income — so the first table of the product this feature exists for was authorable only by hand on the calculation screen, which is the discoverability failure the cards exist to close. It does not contradict spec §10.2: a choice-keyed ceiling printed under a sheet's *Loan Amount — Maximum* is the BANK's cap and still belongs on `loanLimits.maxLoanByFact` (three of those sheets have no rule to hang a path on, and `minOf` cannot cross units); this card is for the product whose whole calculation is a ceiling read off what the customer owns, where both sides are EGP and same-unit `minOf` is what §10.2 explicitly keeps. **(2) The cards are checkboxes, GROUPED by output kind.** A product carries one `outputKind`, so the two groups are the two answers the first pick already implies, and the other group goes unclickable rather than being refused at save — a mixed pick has no representation to store. `?from=` becomes a comma list read by a new `seedFromStarters`: the first key seeds the primary way and the output kind, the rest push way rows, and more than one seeds `combine: 'lower'` per spec §4 Q2 and acceptance test 9. An unknown key is dropped rather than defaulted, and so is a key whose output kind disagrees with the first — a guessed mechanism would seed a shape nobody picked and the form would read as answered. A single value still seeds exactly one way with no combine, so every link written before this lands where it always did. Pick ORDER is preserved end to end and never sorted, because the first way is the `primary` slot and the second is `alt`, and those ids are what a bank's figures are keyed by. **The defect this surfaced, which had shipped:** the `combine` select sat INSIDE `formArrayName="alternatives"`, so Angular resolved it against the array, threw `Cannot find control with path: 'alternatives -> combine'` and never bound it — "Take the lowest" could not be chosen through the UI at all, and the real `compound_owner` product's stored `lower` was invisible on its own screen. The ways now sit in their own wrapper inside the addon body. Found by driving the screen, not by reading it. **`impec` after, with the findings measured rather than eyeballed:** the unavailable cards began at one opacity over the whole card with a comment claiming they still tell the operator what the platform can do — measured at **2.63:1** on the detail line at 0.6 and worse at 0.45, so the claim was false and a sentence nobody can read is not information, while an opacity high enough to reach 4.5:1 stops reading as unavailable. What recedes is now the AFFORDANCE — hollow medallion, dashed border, dashed tick box — and every word keeps its live ink (**6.17:1** light, **8.91:1** dark). They also carry `aria-disabled` rather than the `disabled` attribute, because a disabled button leaves the tab order and the reason bound to it by `aria-describedby` is then announced to nobody; `pick()` refuses the click instead. The tick box moved off the card's vertical centre onto the title's line (centred it sat beside the third line of the detail text, reading as a tick on the sentence), the group label dropped to the house's `text-xs` + 0.06em uppercase micro-label scale, and the cards gained the fourth state they were missing (`:active`). The consequence of two picks is stated as one SENTENCE on the create screen and the `combine` CONTROL stays on the calculation screen — two controls for one field would be two authorities. **Verified in a browser against a real database:** a name created from scratch with two ceiling ways landed on `…/calculation?from=ceiling_by_choice,ceiling_by_class&then=<name>` with the primary, the way row and *Take the lowest* all seeded before any typing; facts picked and saved from the screen; `GET …/template` returned `alternatives` + `combine: 'lower'` and a compiled rule carrying `basis_combine` (`minOf`, `skipUnset`) wrapped in `basis`; the legacy single-value link seeded one way and no combine; a reload read the stored form back with the select bound; the income group dimmed with its reason and its cards refused the click; measured across en / dark / real ar-EG RTL with page overflow 0 and no page errors on every run. Test rows deleted afterwards (the product needed a direct delete — `surrogate_product` is not in `DELETABLE_TYPES` by design). 8 new i18n ids with ar targets and 3 reworded sources; untranslated ids 424 = baseline. Backend 1150 → 1155 green, admin 106 → 108, both locales build, lint on every touched file identical to HEAD apart from the repo-wide `*Page` class-suffix convention. **Not done, stated:** the same shape twice (a unit-price bracket and a down-payment bracket, which two sheets do use) cannot be ticked from the cards, since they are keyed by starter — the calculation screen's *Add another way* still covers it, and the server refuses a duplicate only when kind AND fact match.
- 2026-09-01 (v20.3.0): MINOR — **the six gaps between `docs/surrogate-income-templates-implementation-spec.md` and the code, closed.** The spec was saved into `docs/` and audited against the repo: most of it had shipped (the template layer and its slot-key safety, the fact-keyed max-loan cap with explicit `onNoMatch`, adjustment `scope`, I-Score last with a 100% fallback, `dbrCapPercentByEmploymentType`, the paste importer, AA·AB·A·B·C·Other with explicit `sortOrder`). Six items had not. **(1) §10.6 — the RATE BASIS, which nothing stored.** Every figure the engine had ever quoted was an annuity over a reducing balance; the four Arabic sheets say `DEC` and the ten ABK sheets say nothing. The same instalment buys 22–29% more loan reducing than flat, in the direction of over-quoting. New pure `rate-basis.ts` (`reducing` | `flat`, absent reads as `reducing` because that is what every stored program was priced at — a default of `flat` would silently re-price the catalogue, and an unknown value is refused at the DTO where a typo is still fixable). `pmt.ts` becomes the ONE home for both directions of both formulas — new `maxPrincipalRaw`, and `dbr.ts` deletes its inline annuity inverse rather than keeping a second copy that could invert a formula the forward direction no longer uses. `ceilingToIncome` takes the basis too, or the ceiling → instalment → principal round trip comes back as a different amount from the bank's table. `quote.ts` reads it ONCE beside the rate and passes it to all four call sites. Frozen on the offer (`bank_offer.rateBasis`, nullable, migration `20260901090000`, **no backfill** — every existing row was priced by the annuity and writing `reducing` onto history would claim a decision nobody made). Wizard control drawn as two radios with the consequence written out rather than a checkbox, because "flat" and "declining" are the two words an operator reads past. `rate-basis.spec.ts` pins §13 test 28 to the pound: 205 714 / 266 667 / 305 455 / 320 000 flat against 251 514 / 340 700 / 395 075 / 413 690 reducing at 36 / 60 / 84 / 96 months. **(2) §10.3 — one relationship question was three.** ABK's NTB/Top-up asks whether this is a top-up of a loan you already have; FABMISR's NTB/X-SELL asks whether you hold another product; EGBank's New/Existing asks whether you are known at all. A customer with a card but no loan is X-SELL at one, existing at the second and NTB at the third — one shared axis reads the wrong row at two banks out of three. `bank-relationship.ts` generalises to `BANK_AXES`, three bank-listed MULTI_SELECT questions and three derived facts, each spelling its OWN branch codes (`ntb`/`xsell` · `new_loan`/`top_up` · `other_product_none`/`other_product_held`) so a template keyed on one cannot be repointed at another with its stored branches still validating. `ApplicantProfile.bankRelationshipSlugs` becomes `bankAxisSlugs`, keyed by fact — one field, not two, because two would drift. The admin's three derived-fact surfaces stop each carrying their own copy of the labels and read `DERIVED_FACTS`. **(3) §8 — a CSV can be opened, and a list written back out.** The file fills the paste box (UTF-8 BOM stripped — Excel in an Arabic locale writes one, and it would otherwise become part of the first row's label AND of the key slugged from it — CRLF normalised, trailing newline dropped) and is handed to the SAME parser, so preview, refusals and the class fixer behave identically either way. The export carries **no quoter**, deliberately: `parse-pasted-values.ts` ships none and splits from the END, so `Mivida, Phase 2,ميفيدا,AA` already parses while a quoted field would come back with its quotes as part of the name; a separator inside the two trailing fields is replaced by a space, which an operator can see, rather than quoted, which they cannot. **(4) §10.1 — city tiers as governorates.** A two-option `city_tier` question is forbidden by the spec and it is right: ABK tiers Cairo & Alex against everything else, the Arabic DOCTOR sheet tiers eight governorates, and neither "other" is the other's. Migration `20260901120000` files all 27 governorates under three classes cut so BOTH groupings are unions of whole classes, RAISEs rather than commits if any lands unfiled, and `20260901123000` registers `property_governorate` as a platform fact bound to the question that already exists. No new mechanism — `factParentTable` walks the answer up. **(5) §10.11 — weighted additional income.** The Arabic COMPOUND sheet counts rents at 50%, certificate returns at 75%, fixed allowances at 100%, variable at 75%, capped at 100% of the basic; the platform had a yes/no question and read no amounts. New pure `additional-income.ts`, four optional NUMERIC questions gated on that yes/no (an applicant with none is asked nothing more than today), four platform facts, and a per-BANK policy on `incomeAssumption.additionalIncome` — never a catalog default, because two banks selling one product weigh rent differently. Applied in `quote.ts` AFTER the basic figure (I-Score included, since that is inside the rule) and BEFORE the debt-burden band is chosen: `dbrBands` are keyed by income, so counting rent afterwards caps the applicant on the band of somebody who earns less. The re-resolve carries the RESOLVER's own override decision rather than re-deciding it, so a rule override cannot attach itself to a declared salary. Refused at save with one code and six reasons; a CEILING product is skipped on purpose — rental income does not make a unit bigger. **(6) §12 housekeeping:** the migration comment that delegated to a file deleted before it ever ran now names `purge:handbuilt` and says so; "Edit the wording" on a product's ask deep-links to that question (`?q=<code>`, read once and cleared so a reload cannot re-open a closed form) instead of landing on the whole pool. `credit_card_total_limit` / `credit_card_limit` **stay**: they are a question code and a fact key joined by `boundQuestionId`, and renaming the fact would orphan every stored rule that names it — the §5.4 failure this feature exists to prevent. **Found by exercising it, not by reading it:** `stripForeignMethodConfig` whitelists what survives a save, so the first working additional-income policy was silently discarded — the request succeeded, the screen showed the weights, and the stored program counted none of them. Fixed, then every refusal exercised over HTTP against a real database (`unknown_fact` · `not_numeric` · `percent_out_of_range` · `cap_out_of_range` · `duplicate_source`), the real sheet saved and re-read, and the test program restored to its original state afterwards. 1 new error code (`ADDITIONAL_INCOME_INVALID`, both dictionaries), 22 new i18n ids with ar targets and 9 orphans dropped (untranslated ids 424, at or under the 425 baseline). Backend 1105 → 1150 green, admin 106 green, both locales build, lint on every touched source file identical to HEAD. **Not done, stated:** §10.5 (which of four readings the Arabic COMPOUND formula means), §10.8 (which bank owns the four unlabelled sheets) and §10.10 (the missing DBR percentages and the basis on each ABK sheet) need an answer from a bank, not code; the mobile app is unchanged (the four new questions ride the existing questionnaire, and the offer's basis is not yet rendered on a card).
- 2026-08-31 (v20.2.0): MINOR — **surrogate products move ONTO the program catalog: one section, one sidebar item, and the Surrogate chip stops filtering names and starts listing calculations.** `/program-catalog` listed the 22 catalog NAMES behind an `All · Income proof · Surrogate` chip bar; `/surrogate-products` separately listed the CALCULATIONS a no-payslip name quotes off. Two entries for one question, and the product screens lit no sidebar item at all. **They are not peers and cannot share a grid:** a name and its product frequently share a key (`compound_owner` is both), so one flat list prints that key twice and reads as a duplicate. So a product is a CONTAINER here — its card lists the names taking their calculation from it, each one a link to that name's page. **The hole that shape opens is the load-bearing part:** a name may carry `no_payslip` and link to NO product (`surrogateProductKey === null`), and rendered only inside product cards those names are reachable from NOWHERE. They get a third group, *Not taking a product's calculation* — which on the real database is 11 of the 12 surrogate names, so the merge would have hidden nearly all of them. **New `platform_enumeration` row field `hasOwnIncomeRule`** (the bit, never the rule — nothing on a LIST reads a catalog rule, and shipping the blob would put a bank's cap tables in a response the board needs one boolean from; `findAllOrdered` uses no `select`, so no query changed) splits that group into the two states an operator can act on: *states its own calculation* (legacy, quotes fine) and *works out no income* (quotes nothing, silently). On this database that is `police` and `govt_employee`. Absent on the wire reads as the harmless state, never as broken — inventing a warning from a field an older backend simply did not send would send somebody to fix a name that is fine. `project()` in the controller also stops re-declaring the repository's row as a structural literal, which is what let the two drift. **Routes:** the four product screens move under `program-catalog/products/*`, declared before the single-segment `:key` (a literal `products` would otherwise resolve as a catalog name of that key); `/program-catalog/products` is a **`canMatch` redirect** carrying `?basis=no_payslip`, because `redirectTo` cannot hold a query param AND Angular SKIPS `canMatch` on any route that declares `redirectTo` — so it could not be a guarded redirect route either (same shape as `resolveCohortMatchFn`). Five explicit `/surrogate-products/**` redirects rather than one wildcard: a `**` cannot carry `:key`, and a bookmark to one product's calculation is the link worth keeping alive. The sidebar-highlight trap the old routes file documents dies with the second nav item — one item, default prefix matching, lit on every screen in the subtree including the product pages, which used to light nothing. New `program-catalog.paths.ts` replaces 14 hardcoded path strings across six files (a stale absolute path in Angular does not fail to compile, it fails at the click). **New pure `catalog-board.ts` + 15 specs**: `basesOf` (moved verbatim from the page, product-link fallback and all), the product↔name join, the three groups and the chip counts — six computeds and two private methods that lived on a 1 300-line component where none of it could be exercised, and the case that matters fails silently. It reports a linked name key with no row (`orphanNameKeys`) rather than quietly rendering one chip fewer, and still lists a DEPRECATED name inside the card that quotes through it. Chip counts are taken AFTER search, so "Surrogate 0" reads as "nothing matches" rather than "there are none"; a name sold both ways is one proof card plus one chip inside a product card, so the two sides add up and neither is short. `?basis=` follows the `?step=` / `?loan=` convention (signal is truth, URL mirrors with `replaceUrl`, initial read from the snapshot), so flipping a chip does not fill the back button with filter states. The stat strip trades *Deprecated* for *Surrogate calculations* — the board lists both kinds of object now, and the deprecated tail carries its own count in its heading. **The product card is NOT one big anchor**: an `<a>` inside an `<a>` is invalid and the browser closes the outer one, so the title is the stretched link and the name chips are SIBLINGS lifted above its overlay, the same escape `.row-actions` already uses. `impec` after: the *its own calculation* tag was dropped — it sat on nine cards out of eleven, at which point the row of colour says nothing and the two that need a human are the hardest to find (the same rule the Inactive pill already follows); "No catalog name sells this yet" moved from disabled ink (under 4.5:1, and it is a sentence somebody must read) to a warn tag; a product's empty state says "No bank quotes from it yet", not the name card's "Not offered yet" — a product is not offered to anybody, it is what a program quotes FROM; name chips take 44px targets under `@media (hover: none)` because they are links now. 15 new i18n ids with ar targets, `@@sp.new2` reused verbatim rather than minted again, 16 orphans dropped (the deleted list page's whole `@@sp.*` header set, `sidebar.surrogate_products`, `program_catalog.basis.empty`, `program_catalog.stat.deprecated`); untranslated ids **425 = baseline**. **Verified in a browser against a real database**, not assumed: 6 routes × light / dark, plus the same routes on the REAL ar-EG bundle in RTL, page overflow measured at **0** on every one and exactly one sidebar item lit on every one; the name chip resolves to `/program-catalog/<name>` while the card body resolves to `/program-catalog/products/<key>`; all five legacy URLs land; the back link returns to `?basis=no_payslip`; searching "compound" finds the product through the NAME that sells it; the mirrored arrow measured as `matrix(-1, 0, 0, 1, 0, 0)`. Backend 1076 green, admin 57 → 72, both locales build, and the lint set on every touched file is identical to HEAD apart from the repo-wide `*Page` class-suffix convention. **Found, not fixed:** `lookup-value-list.component.ts` writes a signal from inside an `effect()` and throws NG0600 on every screen that renders the values panel — reproduced on `/lookups`, which this change does not touch, so it predates it. **Data note:** `compound_owner_4` was linked to the `compound_owner` product to exercise the names-inside-a-card path, and the server REFUSES to unlink it (`SURROGATE_PRODUCT_REQUIRED`) — the row was already in a state the API would not create, which is precisely what the new *works out no income* badge exists to surface. **The group the merge surfaced was then emptied by deleting it, on the operator's explicit decision** — new `npm run purge:legacy-catalog` (`scripts/purge-legacy-surrogate-catalog.ts`), a SCRIPT and not a migration for the reason its sibling `purge:handbuilt` states: a migration runs on every deploy with nobody present, and this deletes bank programs. Dry-run by default, printing the whole blast radius; `--confirm` writes, in ONE transaction. Names are listed BY EXPLICIT KEY rather than read from the group, because the group is DERIVED — a name joins it the moment somebody unlinks it, so a script reading it deletes a different set depending on when it runs, and the name somebody is midway through fixing is exactly the one it would take. Ordering is load-bearing: `bank_program.bankId` is `onDelete: Restrict`, so programs go before their bank, or Postgres reports a foreign key instead of the book about to be removed. **The fact that decides whether this is a cleanup or a data loss was verified in the schema, not assumed:** `bank_offer` stores `programCode` and `bankName` as PLAIN STRINGS with no FK to `bank_program` or `bank` (an offer is an immutable snapshot, Principle I / A6), so 532 historical offers naming deleted programs survive untouched, along with every application, decision and saved offer; only `scoring_weight_set` cascades and `audit_event.bankProgramId` nulls. An earlier warning that applications and offers would cascade was wrong and was corrected before the run. The dry run also caught that the kept banks lose 10 MORE programs to the name purge than the plan implied (39 of 50, not 29) — surfaced before writing, not after. Run here: **13 → 3 banks** (ABK Egypt, CIB, Bank NXT), **50 → 11 bank programs**, **22 → 11 catalog names**, and the *Not taking a product's calculation* group **11 → 0**. The keep-list refuses rather than proceeds if a name in it matches no bank, so a typo cannot delete every bank instead of nine.
- 2026-08-29 (v20.1.0): MINOR — **a list is loaded by PASTING it: one request, one transaction, one questionnaire publish. The compound lists are back on, and an untick has somewhere to land.** The mechanism for "every compound in Egypt, each filed under a class" already existed — `product-fact.page.ts` authors the pair, `parent-class-board` files them, and the v20.0.0 `ceiling_by_class` starter compiles to `factParentTable`. What did not exist was any way to USE it past a handful of rows. Entry was one value → one `POST`, `admin/enumerations` carries no `@SkipThrottle()` while the global limit is 100 per 15 minutes, and — once a list is mirrored — every value write republishes the whole questionnaire, each snapshot embedding every option of every question, so the JSON written grows as the SQUARE of the list. Four hundred compounds was four hundred requests, four hundred versions, and a half-written list at row 101 with `product-fact.page.ts`'s own catch admitting "whatever landed, landed". **New `POST admin/enumerations/values`** (declared before the `:id` routes, like its two bulk siblings): ALL-OR-NOTHING with EVERY bad row reported at once — `meta.problems[].index` is ZERO-BASED and the screen adds one — one `createMany`, one new `AuditEventWriter.writeMany` (which also retrofits `setParentKeysBulk`'s 500 sequential inserts), one `invalidateCache()` with no argument (new children of a class list change what `surrogate_fact`'s derived `parentOptions` answers — the reasoning `update()` already writes out and single-row `create()` gets wrong), then **one** `syncMirroredList`. Keys are slugged from `labelEn` with **`slugify`, never `uniqueSlug`**: `_2` on collision mints two rows no picker and no bank table can tell apart and destroys re-paste idempotency, so a collision is reported as the duplicate it is — which makes re-pasting the same sheet write nothing and publish nothing, verified as 0 created / 71 skipped / 0 new versions. A batch where every row is a duplicate returns before the transaction, so a habitual re-submit never reaches the sync at all. Refused for `program_name` / `surrogate_product` / `surrogate_fact`, whose values carry settings a three-column paste has no column for. `plan.update` in the mirrored sync becomes one `UPDATE … FROM (VALUES …)` chunked at 1000 (Postgres caps bound parameters at 65 535 and this binds four per row) with `"updatedAt" = now()` EXPLICIT, because Prisma's `@updatedAt` does not fire on raw SQL; `publish()`'s per-question `optionsByQuestion` N+1 becomes one `optionsByQuestions`; and `versionHistory()` gains `select` (no `snapshot`) + `take: 50` — verified safe first, the admin's `QuestionnaireVersionRow` has no such field. **Migration `20260829090000`** deletes the stray `test_compund` BY EXPLICIT KEY and before any insert, seeds six classes as **`compound_tier_*` and not `compound_class_*`** — `20260823130000` created `compound_class_a` as the TOP of three (6M) where under six classes "A" is the third, and those rows were deleted outside any migration, so reusing the key would revive a currently-DEAD stored table into a silently wrong figure frozen onto `bank_offer.collateralCeilingEGP`; fresh keys keep every stale row loud, which is the argument that file spends its own header making — adds a customer-pickable `compound_other` filed at the lowest class, brings both kinds live with `compound` deletable, folds every leftover onto the catch-all, and RAISEs rather than commits on any of four half-states. **`enumeration_type_def.fallbackParentKey`** (on the CHILD kind, no FK, same posture as `parentTypeKey`) is read by `resolveParentKey` under `allowUnfiled` and NOWHERE else: an operator's untick is a decision the kind can answer, a blank column on a pasted row is a typo, and answering a typo with a price tier is what `ENUMERATION_PARENT_REQUIRED` exists to refuse. `allowUnfiled` KEEPS its meaning and its single caller — with no fallback declared `null` still stores, or an operator-made axis becomes un-unfileable. **A real defect found by exercising it:** `setParentKeysBulk` validated through `resolveParentKey` and then wrote `dto.assignments` unchanged, so the redirect never reached the database; the resolved answer is now what is written. Two guards: `assertFallbackUsable` at set time, and a retire guard that fires on a class holding NOTHING — which is precisely the case `ENUMERATION_HAS_CHILDREN` cannot see and the dangerous one, since an empty retired fallback is where the next untick lands and the parent walk never checks the parent's own active flag. **`BoundQuestion.parentAxisType`**: `parentOptions` was derived from DATA (the parents the options happen to reference), so a list with an axis and nothing filed reported no classes — hiding the class mechanism at the moment a product is authored — and 400 values across 4 of 6 classes left the bank unable to state a figure for the other two, making "two classes have no row" unreachable for exactly the classes that need it. The axis pass reads `parentTypeKey` off the registry and wins where it resolves; the data walk stands unchanged for `military_grade` and `professor_rank`, which declare none. That unblocks both **`INCOME_RULE_CLASS_ROW_MISSING`** — a `factParentTable` walk added to `incomeRuleReadWarnings`, which until now only ever looked at the LEGACY root-level `keyTable` and never at `stepParams`, shared with the save path so leaving the screen does not lose it, and landing in `warnings[]`, which IS rendered (CLAUDE.md's "read by no screen" line was stale) — and **the `classTable` fact filter**: `factsOfType` keyed on question type could not tell `choiceTable` from `classTable`, both SINGLE_SELECT, so the class shape offered facts whose answers are filed under nothing and then answered `no_matching_row`, which is FATAL. Now `factsFor(kind)`, predicate `parentOptions.length > 0` to match what `keyOptionsFor` actually consumes, with `altKind`'s default moved off `classTable` (which the filter would have opened empty). Warning only — **`income-rule.validator.ts` is untouched**, its comment on parent keys is right: refusing on a class list that moved refuses a save another screen makes valid. **Admin:** new pure `parse-pasted-values.ts` (21 specs) — tab-first decided per DOCUMENT not per line, split-from-END so `Mivida, Phase 2,ميفيدا,AA` parses with no CSV quoter, and class matching in three passes where the third compares `labelAr` as TEXT because `slugify('فئة أ')` is the empty string and every Arabic class token would otherwise collide on it, a bug that only appears in the primary locale; a missing class is amber and counted, a MISTYPED one is red and blocks, because sweeping `Clsss B` into the catch-all files a Class-B value at the catch-all figure with nothing saying so. New `/lookups/paste` on `app-form-page` (v19.1.0's size test, met on every clause), reached from the values panel and hostable by the product screen, with a summary strip as the primary surface, a per-token fixer where one pick fixes every line carrying that spelling, per-row inline edit on EVERY row through a line-keyed overlay dropped only when that line's raw text changes, and a server refusal painted back onto the line it names via a `line` the draft CARRIES rather than a label match. `product-fact.page.ts`'s per-row loop becomes one `createValuesBulk` and its class `sortOrder` becomes 1-based, matching the rank the board renders. Scale: a 100-row window on `lookup-value-list` (~2 000 tooltip/popconfirm instances at 500 rows → ~400 on first paint; CDK virtual scroll rejected — rows are variable-height by documented design and `autosize` is in `cdk-experimental`, which is not installed), a 200ms debounced `appliedQuery` on the board, a 200-candidate render cap, and per-class counts in ONE pass. Reactivation: two NESTED checkboxes on the type drawer (two flat booleans give four states, two of which look identical on the rail) and a "Show retired lists" toggle, with a deep link to a retired list finally landing ON it. **Three defects found by measuring rather than reading:** `blockReason` did not cover broken lines, so a sheet with 5 good and 2 broken would have sent 5 and dropped 2 in silence; reactivating `compound_category` put its `gold` icon on the rail for the first time and ng-zorro threw rather than rendering the blank the schema promises, so the page now registers every icon the REGISTRY can name and not the five that happened to be railed; and the first browser run "proved" the token fixer wrote the wrong class, when `hasText: 'Class A'` had substring-matched `Class AA` — the harness was wrong, not the fixer. `impec` after: the preview gained a sticky column header (six bare inputs named only by `aria-label` read as unfinished to everyone who can see), a redundant state dot beside the word (the word is searchable and readable, the dot is what the eye lands on at two hundred rows — neither carries the state alone), line numbers and derived names moved off tertiary ink, a real hit area under the 28px row button, and the two peer buttons in the values header became a link plus a button, because two identically-drawn peers say nothing about which is the everyday case. 5 new error codes (213 in sync), 89 new i18n ids with Arabic targets and 3 retargeted; untranslated ids 425 = baseline. **Verified against a real database and a real browser:** 71 compounds loaded through the endpoint, 20 more onto a MIRRORED list producing exactly ONE new questionnaire version where the old path would have produced 20, a re-paste producing zero, option codes equal to registry keys with 0 mismatches, an untick landing in the catch-all and audited as a real move between two classes, the fallback class refusing to retire, and the token fixer writing the picked class to both its lines and nothing else — then the test rows deleted through the API so no orphan options remain. Browser-driven across en / RTL / dark with page overflow measured at 0 and 0 page errors on every run, plus the real ar-EG BUNDLE confirming every new string renders and `الفئة AA` resolves. Backend 1035 → 1059 green, admin 36 → 57, both locales build. **Deferred, written down:** the mobile picker builds every option at once (`shrinkWrap: true`) and re-filters inside `build` on each keystroke — fine at 71, must be fixed before hundreds go live; per-bank class MEMBERSHIP is inexpressible, because `enumerationParentKeys()` is one flat type-less map with no bank dimension, so this scheme is the PLATFORM's and banks price against it; deleting a class key still silently orphans a bank's stored figures (`countGenericReferences` counts children only); a retired value still hard-rejects a returning applicant's resubmit; and there is still no mirror re-sync endpoint despite the service doc claiming one. Single-row deletes still publish one version each — only the create path was batched.
- 2026-08-28 (v20.0.0): MAJOR — **a no-payslip product is AUTHORED by answering three plain questions; the graph editor becomes Advanced, and it is one-way.** v19.0.0 made the calculation admin-authorable and left the only door a graph editor asking bank-operations to pick "operations", wire `ValueRef{step|fact|const}` between steps, and know that step 3 must point back at steps 1 and 2. That is programming. Nine banks across five products all fit ONE frame with three switches, so the form asks about the frame: *how the income is worked out · what else applies · what the numbers are*. New pure `matching/pipeline/product-template.ts` compiles it to the SAME fourteen ops (**no new op**) and `product-template-starters.ts` holds seven starter shapes — **shapes only, no words, no figures, no bank names** (one product is sold by many banks off one mechanism; a bank in a starter key would be a hardcoded bank, Principle II / A1; the admin supplies the words in both locales and the worked example beside each card is help text, never saved). Migration `20260828090000` adds nullable `platform_enumeration.templateSpec`; `incomeRule` stays the compiled artifact and both are written in ONE statement, so `effectiveProgramNameRule`, `effectiveIncomeRule`, the snapshot mapper and the engine are untouched. **Step ids are SLOT NAMES** (`primary` · `primary__<branch>` · `alt` · `basis` · `uplift` · `iscore_*` · `src__<fact>` · `cond__<id>`), never indices — a bank's `stepParams` and its `valueSources` markers are keyed by them — and the FIRST column keeps the bare id so turning a one-column table into two never moves a figure that is already there. **`PRODUCT_TEMPLATE_ORPHANS_FIGURES` (409)** refuses a recompile that would strand a figure some bank has typed, naming the programs and the boxes (two hops, product → names → programs, via new `programFigureKeysUnderProduct`); the product's OWN defaults are carried and pruned instead of refused, because the operator can see and re-enter those and cannot see a bank's. **The design doc was wrong on two points and both changed the build.** (1) Its I-Score mapping (`factNumber → bandTable → coalesce[…, {const:'100'}] → percentOf`) cannot deliver the fallback it promises: `factNumber` on an unanswered fact answers `fact_not_answered` and the evaluator RETURNS before the `coalesce` is reached, so one skipped optional question would kill every quote for the product. `pickByFact` already solves this shape and matches an option CODE, which a numeric score cannot drive. So ONE narrow engine change — **`RuleStep.optional`**, honoured only by the three fact ops, downgrading an absent answer to `rule_unconfigured` (skippable) instead of `fact_not_answered` (fatal). A field, not an op, so "no new operation" holds and "no engine change" does not. Fenced at save by new `optional_step_not_skippable`: every path from an optional step to the answer must pass through a `coalesce` offering a NON-step alternative — another step reading the same answer is no fallback, because the absence unsets both. (2) `productRuleHasError` is NOT dead code (the bank wizard calls it at `bank-program-form.page.ts:4515`); what is true is worse and narrower — `surrogate-product-detail.page.ts` and `program-name-detail.page.ts` had NO client-side gate on a pipeline at all. Wired in, not deleted. Also not a defect: `credit_card_total_limit` is a QUESTION code and `credit_card_limit` a FACT key, joined by `boundQuestionId` — commented on both admin constants, changed on neither. **I-Score** is one platform-owned fact (`surrogateProductKey: null` — it is a property of the applicant, not of one product), a NUMERIC optional question seeded in `seed-questionnaire.ts` (300–900, all four categories) because that file already publishes and a migration cannot; stored as a NUMBER so a future bureau feed reuses the same field. It compiles LAST, after the product's own adjustments, because `percentOf` rounds to 2dp at every step — multiplication commutes, the rounding does not — and inside the rule, which is what puts it before `resolveDbrCap` **by construction**: the cap can itself depend on income, so a program-level multiplier would land after a band was picked from the un-multiplied figure. Pinned at the band edge (46 000 × 110% = 50 600 must land in the UPPER band). **`dbrCapPercentByEmploymentType` reaches an operator for the first time**: the engine has read it since v18.1.0 and it was absent from `eligibility-config.dto.ts`, so `forbidNonWhitelisted` rejected it on the wire and no editor offered it — every second sheet says "50% salaried / 40% self-employed" and it could not be entered. New `COARSE_EMPLOYMENT_TYPES` + a validator that refuses an unknown BUCKET (a cap the engine never looks up reads as configured and is dead at quote time) and a value outside `(0,100]` (worse than dead — `resolveDbrCap` re-checks and falls through, so a `0` looks like "capped at nothing" and quietly quotes the flat rate); two-row editor beside `dbrBands` in wizard step 5. **Screens** (`promax` before, `impec` after, Principle XXIII): `/surrogate-products/new` — seven shape cards, `role="radiogroup"`, one plain sentence and a worked example each, no colour-coding (seven accents would imply a taxonomy that does not exist); `/surrogate-products/:key/calculation` on the `app-form-page` shell, three sections on the page GROUND (not cards inside cards — only the shape choices are cards, because those are discrete pickable items), add-ons revealing their own fields inline, and the figures editor reusing `app-product-rule-editor` verbatim, hidden while the answers above are dirty with a line saying why (compiling here to preview would be the second authority this design exists without). **Four a11y findings fixed by measurement, not by reading:** the add-ons were `nz-switch` inside a `<label>`, and ng-zorro renders a bare `<button>` with no role, no `aria-checked` and nothing to label it — the label associated with NOTHING and a screen reader heard four unlabelled buttons. Moving `aria-labelledby` onto the host did not help (the host is not the control, verified in the DOM); a clickable `<span>` created a second tab stop for one control. They are native checkboxes now, which is also the honest control for "tick as many as apply" and what the source design draws. Also: the sticky action bar covered the foot of every form it shipped on (`app-form-page`, so `product-fact` and `new-question` too) — the body now reserves its height. **Your decision, executed as asked and stated plainly: every hand-built calculation is deleted.** New `npm run purge:handbuilt` — a script, not a migration (a migration runs on every deploy with nobody present) — dry-run by default, printing the whole blast radius, `--confirm` to write and a separate `--force` for questions real customers have answered (`application_answer.questionId` is RESTRICT, so it would fail anyway; better to say why). It also finishes the job migration `20260827090000` delegated to `seed-collateral-products.ts#pruneRetiredDemoProducts`, deleted in `ad6d6d0` before it ever ran. Run here: 2 products, 3 questions, 0 bank programs, 0 customer answers. **Housekeeping:** three operator-facing strings naming deleted scripts (`surrogate-products.page.ts:101` + its stale `ar` target, `product-rule-editor.component.ts:180`, `seed-program-catalog.ts:444`), and `product-fact.page.ts` returning to `?step=2` while its own comment said it returned to "What it asks" (1-based, so 1). Adding an ask was create-only — per-row **Remove** now unbinds the fact, deletes the registry row and soft-deletes the question, in the only order the FKs allow. 3 new error codes (208 in sync), 117 new i18n ids with ar targets, 2 orphans dropped, untranslated ids 425 = baseline. **Verified against a real database, not assumed:** every refusal exercised over HTTP (unknown table key, `mechanism_needs_fact`, `baseline_dbr_on_income`, orphaned figures naming `ABK-BIZ-WORKING_CAPITAL` and `lostKeys: ['half']`, and the full Advanced round trip — hand-edit clears the form, the form then refuses to overwrite it, the screen is told why); I-Score live in snapshot v96; browser-driven across 5 routes × light/dark/RTL with page overflow measured at 0 and 0 page errors on all 15. Backend 958 → 1035 green, admin 36 green, both locales build, and the lint set on the touched files is identical to HEAD apart from the repo-wide `*Page` class-suffix convention. **Not done, stated:** the form cannot describe a shape outside the seven mechanisms — Advanced stays for those and the screen says the switch is permanent; and the sheets' *other income capped at X% of imputed*, *max tenor by employment*, *instalment ≤ 50% of the existing car instalment* (computed AFTER the rule) and *no late payment in the last 6/12 months* are still not expressible.
- 2026-08-27 (v19.1.0): MINOR — **an add/edit form is a SIDE SHEET, not a modal; a form too big for a sheet gets its own SCREEN.** Six forms opened as centred `NzModalService` dialogs, each capping its own width by hand (`520`, `560`, `min(640px, …)`, `min(880px, …)` — four numbers for one decision) and each drawing its own title, its own action bar and, in two cases, its own header medallion. A centred modal also covers and dims the list the form is edited against, which is the thing the operator was comparing to. Two new shells in `shared/ui/`, presenting the same three parts in the same order so moving a form between them changes the container and not the vocabulary: **`app-form-drawer`** (medallion + title + subtitle + close · scrolling body · footer pinned to the bottom edge carrying the hint and the two actions) with `openFormDrawer()` for the house configuration, and **`app-form-page`** (back link · page header · sticky action bar whose start side carries the outcome or the reason Save is refused). **The line between them is the SIZE of the form, not its importance:** a bounded set of fields is a sheet, where the list stays on screen beside it; a form that branches on a type choice, grows a `FormArray`, or asks twenty questions needs the viewport and a URL to come back to — squeezed into a sheet it becomes a second scrollbar with no way back after an interruption. Converted to sheets: the lookup VALUE and lookup TYPE editors, the bank form, the staff-user form, the catalog program-name editor and the surrogate-product create (`*.dialog.ts` → `*.drawer.ts`, `NZ_MODAL_DATA`/`NzModalRef` → `NZ_DRAWER_DATA`/`NzDrawerRef`). Promoted to screens: `product-fact.dialog.ts` → **`/surrogate-products/:key/asks/new`** (20 controls, 5 `FormArray`s, four writes) and `new-question.dialog.ts` → **`/program-catalog/:key/questions/new`** (1 650 lines, 15 controls). Confirm dialogs stay modals — a delete confirmation is not an edit. **Two things the promotion had to solve, not paper over:** the new-question form's context was three fields handed over at open time, and two of them are not on the URL — the CATEGORY travels as `?loan=` (synchronous, because it seeds the category pick and half the wording), the name label and the pool codes are read by the screen itself (async, so the subtitle and the slug preview fill in when they land, never a placeholder name); and its RESULT is consumed by the name's page, which owns that template — so the created question travels back as **router state, taken once** (`NEW_QUESTION_STATE_KEY`, cleared with `history.replaceState` so a plain reload cannot re-tick), and the tick is still written by the page that re-reads the current pick set before writing it. **`nzPlacement` is resolved from the document's own `dir`** rather than left to the CDK: the Arabic build sets it at runtime, and a hardcoded `'right'` opens the sheet on the START edge there. Mask clicks do NOT close a sheet (everything opened this way holds unsaved input); Esc still does. **A real defect found by A/B measurement, not by reading:** the shells originally used `nz-icon` with their own `provideNzIconsPatch`. `NzIconDirective` injects the NEAREST `NzIconPatchService` and patches only that one's set, and a PROJECTED node resolves the shell it was inserted into before the component it was written in — so a patch on a shell shadows the patch of every form rendered inside it. Measured with the patch in place: 7 blank icons on the new-question screen, 1 on the product-fact screen, a blank medallion on two of the six sheets; measured without: 0 everywhere. Both shells now draw their own glyphs as inline SVG, the back arrow mirrored by `:host-context([dir='rtl'])` rather than kept as a second path. (The first reading of this was wrong and was thrown away: the initial "7 blank icons" measurement came from a stale bundle behind my own template error, and the diagnosis only became true when it was re-measured on a healthy one — in both directions.) **Also from exercising it:** a sheet stopped at its content height with the footer floating mid-viewport, because the form component sits BETWEEN antd's drawer body and the shell and a percentage height cannot see through a `display: block` ancestor of automatic height — closed with one global rule keyed to `openFormDrawer`'s own wrap class, and nothing else uses it. antd focuses the first focusable node in a drawer, which in this layout is the CLOSE button, so someone who opened "Add bank" to type a name tabbed past a Close first — the helper now lands the caret in the first TEXT field on `afterOpen` (not in the shell's `ngAfterViewInit`, which runs before the open animation and loses to antd's own call; text fields only, since focusing an ng-zorro picker is one keystroke from opening a list over an unread form). **impec pass:** the footer hint and the action bar's outcome line move from tertiary to secondary ink (tertiary sits under 4.5:1 at that size, and both lines are read, not decoration); the medallion and the close button take one new `--size-sheet-head-box` token so the title sits on a single optical line between them; the sticky bar drops its shadow — every shadow token in this theme casts DOWNWARD and a bar pinned to the bottom edge needs the lift above it — and takes the body's own max width so the two edges agree. **Copy fixed in passing:** `bank-form` rendered `'Add bank'`/`'Edit bank'` as hardcoded English (A20) and wrapped an interpolation in a single `i18n` tag, so the Arabic build showed English on both; each sheet now names its own primary action ("Add program name", "Create product", "Create bank") instead of the generic "Add value" the mechanism would have given it. **Verified in the browser on every converted surface:** all six sheets open from the end edge at 560px with 0 page overflow, 0 blank icons and 0 page errors, in light, dark and RTL (RTL opens from x=0); a lookup value edited → saved → re-read after a reload → reverted; the sheet body scrolls with the footer pinned; both screens deep-link, resolve their subtitles from real keys and return to the tab they came from. 24 new i18n ids with ar targets, 2 orphans dropped; untranslated ids 428 → 425 (measured against a worktree of HEAD). Admin 36 tests green, both locales build, prod build green, and the lint set on the touched files is byte-identical to baseline (the repo-wide `*Page`/`*Drawer` class-suffix convention). **Not done, stated:** `promax` was not run before these screens — the design came from a reference the user supplied, which is a tighter spec than a generated system; `impec` was run after, as Principle XXIII requires. The reset-password and delete-program dialogs stay modals.
- 2026-08-26 (v19.0.0): MAJOR — **a surrogate product AUTHORS what it asks: the list, the question and the fact, from its own screen. The compound demo is deleted.** v18.4.0 made the no-payslip product a first-class row and stated the gap it left: what a product READS was still three rows in three tables that only a seed script could make line up. The join is one equality — `question_option.code` === `platform_enumeration.key` — which the engine keys a bank's table by (`factChoiceTable` / `factParentTable`) and which `attachOptionsEnumerationType` recognises a question's list from. The admin option API cannot produce it: it mints a code by slugifying a label. So "add a no-payslip product" was a release, and the compound demo existed to teach that way of working. **Migration `20260827090000_surrogate_product_authoring`** adds two nullable columns to `enumeration_type_def` — `surrogateProductKey` (which product AUTHORED this list; provenance, gating nothing) and `mirrorQuestionId` (FK → `question`, `ON DELETE SET NULL`: the question whose options ARE this list) — and widens `platform_enumeration.surrogateProductKey`, already indexed, to `surrogate_fact` rows as well as `program_name`. **Two links, because they answer different questions and each is silent exactly when the other is not:** the stored one says what the product MADE, which is the only answer available before there is a rule; the derived coverage inference says what the finished calculation READS, including lists somebody else owns. **`mirrorQuestionId` is stored rather than derived and lives on the LIST, not the question**, for one reason each: the sync runs when a VALUE is written and a value write knows its type and nothing else; and coverage breaks the instant an operator adds the tenth value, which is precisely the moment there would be nothing left to read the link off. **`CreateQuestionWithOptionsDto.optionsFromEnumerationType`** expands a type's active rows into options inside the existing one-transaction create (code = key, order = `sortOrder`), then stamps the link; mutually exclusive with `options` — two authorities for one list is how they disagree, and a merge would put slugged codes beside registry keys inside one question. **`QuestionnaireService.syncMirroredOptions`** re-runs after ANY value write of a mirrored kind, so a compound typed a week later reaches the served questionnaire with no seed run; it DEACTIVATES a retired value's option rather than deleting it (an answer, an archived snapshot and a bank's key table all reference that code) and RESURRECTS the same row when a value returns. The diff is a pure `mirroredOptionPlan`, unit-tested, because a diff only exercisable through a Prisma transaction is a diff nobody exercises; the plan is also the publish gate — a no-op write mints no version, since version history is how an operator reads what they did. **The module cycle is real and three hops** (`platform-enumerations → questionnaire → customer-auth → platform-enumerations`): every edge needs `forwardRef`, and a plain reference at any point in a cycle resolves to `undefined` for whichever module the loader reaches first — Nest reports that as "the module at index [2] is undefined", which reads as a typo rather than as the cycle it is. **Admin:** the product page's steps become ① What it asks ② How the income is worked out ③ Who uses it, and step ① is the screen — the product's own facts with their questions, the lists behind them (owned first, class lists after what is filed under them), and a new `shared/lookups/product-fact.dialog.ts` that writes list → values → question → fact in the only order that works, naming each step as it goes so a partial failure says where it stopped. A class list is asked for UP FRONT because a value of a filed-under kind is born filed (`resolveParentKey` refuses a create with no parent), so the board can only MOVE what is already filed. **`parent-class-board` loses its compound defaults** (required inputs now — the default outlived the product it was for) and its 34 strings move to `@@clsb.*` with generic wording; the previous draft argued that Arabic plural, verb agreement and pronoun suffix all agree with a specific noun so the copy could not be parameterised, which is true and is exactly why the strings now substitute no noun at all. **Deleted:** the whole compound-ownership demo — nine compounds, three classes, twelve questions, twelve facts, `COMPOUND_RULE`, the `compound_owner` name + product pair, five bank programs, `specs/compound-owner-rule.md`, `compound-class-registry.spec.ts`, and every compound assertion in `check:collateral` including the two registry invariants (which asserted the shape of a list nobody seeds any more). `car_owner` survives as the one worked example. The two `enumeration_type_def` rows are **deactivated, not deleted**, and the first draft got that wrong three ways: `deleteType` refuses while values survive, operators DO add values (a dev box already had one) with no remedy to offer them since a value's `type` is immutable through the API, and `typeDefinitions()` deliberately returns inactive kinds so a surviving value keeps its parent axis. **Two defects found by exercising rather than reading:** the authoring dialog could not be submitted on EITHER branch — Angular validates controls a template does not render, so the `values` rows kept the form invalid while filling in a number and the `classes` rows did the same whenever grouping was off, leaving a dead Save button with nothing on screen to explain it (fixed by enabling only the visible branch); and `surrogateProductKey` was missing from the staff-picker projection, so every fact reported belonging to no product. **A leak closed with it:** `program_name` IS on `CUSTOMER_READABLE_ENUMERATION_TYPES`, and that endpoint returns the repository's admin-shaped member — so the new column would have shipped each catalog name's archetype to any authenticated customer; the mobile projection now strips it. Also fixed: `EnumerationTypesService.load()` was reachable only from `refresh()`, whose callers are all on `/lookups`, so on a product page the cache was empty for the session and `parentTypeOf()` returned `null` — the values panel and the edit dialog silently dropped the "Filed under" control and the screen looked finished. Six WIP files reached for tokens the theme does not define (`--color-primary`, `--color-danger`, `--color-border-subtle`, `--color-accent-default`, `--color-text-on-accent`), the same class of defect v18.1.2 fixed once. **Verified by rebuilding the deleted demo from nothing through the API and a second product entirely through the UI:** option codes came back equal to the registry keys, a fourth value added later reached the live snapshot with no seed run (v89 → v90) while a write to an unmirrored list published nothing, and the real `quoteProgram` returned 6M / 4M / 2M by class with `fact_not_answered` when the answer was withheld. Backend 913 → 958 green (+45, incl. `mirrored-options.spec.ts` and `mirrored-question-options.spec.ts`), admin 36 green, `check:collateral` green on the car product at 520 000 / 480 000, both locales build with 428 pre-existing untranslated ids (down from 434) and none from this change, LTR + RTL + dark measured at 0 page overflow on every route. **Found, not fixed:** `lookup-value-list` renders `labelEn` in both locales, so an Arabic operator reads their own values in English — pre-existing, and shared with the Manage-values screen.
- 2026-08-25 (v18.4.0): MINOR — **a no-payslip product is a ROW, not a copy: `surrogate_product` archetypes, and the compound lists move onto the product that reads them.** Two problems with one root cause — the platform had no name for "a no-payslip product", so the pieces of one were scattered. (1) Compounds and compound classes lived under `/lookups`, three clicks from the only calculation that reads them: an operator filing a compound under a class had no way to see which product they had just re-priced. (2) v18.0.0 shipped with the gap stated — *"the catalog variant does not yet AUTHOR a pipeline"* — so a new no-payslip catalog name was created silently broken and stayed that way until somebody wrote a seed. Both close if the archetype is a first-class row. **New enumeration type `surrogate_product`**, reusing the EXISTING `incomeRule` + `valueSources` columns (so `catalogIncomeRulePaths` keeps its `incomeRule.` root and NO value-source marker is re-rooted, unlike `20260823130000`). New nullable `platform_enumeration.surrogateProductKey` + index — NOT `parentKey`, for three reasons that are each load-bearing: `resolveParentKey` REQUIRES a parent on create (every payslip name would become uncreatable), `enumerationParentKeys()` is one flat type-less map read by `factParentTable` (a colliding key would hand a compound a class), and the admin mirrors `PARENT_TYPE_BY_TYPE` to decide whether to render a required "Filed under" select. No FK — the reachable unique is the composite `(type, key)`, same posture as `bank_program.programNameKey`. **THE SINGLE SEAM is `programNameIncomeRules()`**: all five callers hand the finished map to `toBankProgramSnapshot` or `.get()` it, so resolving there left the engine, the mapper and every caller untouched (verified). One new pure `effectiveProgramNameRule` in `income-rule-inherit.ts` — **STRICT REPLACE, never a merge**: a merge would be a third inheritance level on a two-level system and would make "the calculation is read from the product every time" false on day one. TWO maps in the resolve, not one: a name and its product deliberately share a key (`compound_owner` is both), so a single map keyed by `row.key` would let whichever row the driver returned last silently win — pinned by a test where the two hold different rules. **Migration `20260825090000_surrogate_product_link`** creates the two pipeline archetypes by copy, links BY EXPLICIT KEY (never "whatever had a rule"), NULLs the linked names' own rule and markers, then RAISEs on four states: a dangling link, a name holding both a link and a rule, an archetype whose `steps` did not come across, and a live `bank_program` that would quote nothing. Deliberately does NOT raise on "any no-payslip name is unlinked" — `catalogIncomeBasis` makes every `business` pair no-payslip, so that would abort deploy on a healthy database. Grandfather what exists, enforce on the next write. **The curated library is a SEED** (`npm run seed:surrogate-products`), not a migration, and it is curated on purpose: six of the eleven `CATALOG_INCOME_RULE` entries are `{"strategy":"declared"}`, so one archetype per name would be six byte-identical products in a picker with nothing on screen saying so — they share one `declared_income`. Existing names are NOT re-linked. **Three dangerous dependents fixed before anyone could run them:** `seed-program-catalog.ts` axis 3 writes `incomeRule` back per name and would have un-NULLed every linked name on the next `seed:catalog`, silently recreating the fork the migration raises on (the four archetypes it touches — `professional`, `doctor`, `professor`, `self_employed` — are exactly the ones the library seeds); `collateral-product-check.ts` builds its own catalog map straight off `incomeRule` and went RED on a correct database (26 failures, confirmed, then fixed); `income-proof-conflicts.ts` read NULL as "states nothing" and told the operator to write a rule back onto the name. **Three new error codes** (196 in sync across backend + both dictionaries): `SURROGATE_PRODUCT_REQUIRED` (422, raised in create AND `setIncomeBases` AND on unlink, so the order the basis and the link arrive in cannot decide validity), `SURROGATE_PRODUCT_IN_USE` (409 — separate from `ENUMERATION_HAS_CHILDREN`, whose meta and Arabic describe a filing relation this is not; load-bearing because the read path resolves a link WITHOUT checking the product's active flag, deliberately, so retiring one cannot blank a live name's income mid-flight), and `PROGRAM_NAME_RULE_LINKED` (422 — without it the write dies as `PRODUCT_RULE_INVALID / no_steps`, naming a missing step list that was never missing). `surrogate_product` stays OUT of `DELETABLE_TYPES`: `ENUMERATION_DELETE_NOT_SUPPORTED` already says the right thing and `deletable:false` already hides the button. **The fact→list link is DERIVED, never stored** — `attachParentOptions` already resolved option codes → enumeration rows and already SORTED by `type` while discarding it; now it carries it (`attachOptionProvenance`, two derived fields on `BoundQuestion`). A stored column would be a third statement of a twice-stated fact, on the wrong row (the QUESTION owns its options), drifting the moment an operator adds a non-enumeration option. Coverage-of-all-options, and ambiguity resolves to nothing — verified live: `compound_name → compound / compound_category`, with `military_grade` and `academic_rank` falling out for free, which is why the product screen has zero product knowledge. **Security, found in passing and closed here:** `GET /v1/platform-enumerations/:type` took the type as a path param and validated NOTHING, with `Cache-Control: public, max-age=300` — any authenticated customer, any type. Survivable while every type held labels a customer sees anyway; not survivable with `surrogate_product`, whose rows carry bank cap tables. New `CUSTOMER_READABLE_ENUMERATION_TYPES` allow-list (the three the Flutter client names), checked BEFORE `isAvailable()` so an off-list type does not get a "try again" 503. **Admin:** new `shared/lookups/` (also fixing an existing cross-feature reach from `program-catalog.page.ts` into `features/lookups/components/`) with the moved value list + edit dialog, a new SMART `app-lookup-values-panel` owning the reload/optimistic-toggle/delete/dialog quartet lifted off `lookups.page.ts` — a panel, not a store, because a product screen needs N types at once and page-level signals cannot express N — and `app-parent-class-board`, the class board with `childType`/`parentType` as inputs. **The 36 `@@ccb.*` strings stay compound-worded**: `"٤ كومباوندات ليس لها فئة، فلا يستطيع أي بنك تسعيرها"` agrees plural, verb and pronoun suffix with that noun, and a 36-field `Copy` interface with one implementer would not spare the second axis a single string. Two new screens on the shared `app-wizard-steps` rail, non-linear like the catalog name's: `/program-catalog/products` (no money on a card — what a product pays is per BANK, so a figure would be a lie the moment a second bank configures it; no colour-coding, which would imply a taxonomy that does not exist) and `/program-catalog/products/:key` (① the calculation ② **the lists it reads** ③ who uses it, walking product → names → programs so "what breaks if I change this" is answerable before changing it). `compound` + `compound_category` leave the `LOOKUP_TYPES` rail; `/lookups/compound-classes` becomes a `canMatch` redirect (a live bookmarkable URL, and `redirectTo` cannot carry `?step=2`). The catalog name's step ① goes read-only when linked and points at the product. Both route additions had to be declared BEFORE their sibling `:key` / `:programCode` param routes — the backend one was caught only by exercising it (`BANK_PROGRAM_NOT_FOUND` for `surrogate-products`). **Verified end to end, not assumed:** every refusal exercised over HTTP; `check:collateral` green at the real ceilings (6M/4M/2M by class, 4.5M top-up, CAE's 300k→240k haircut) including a `seed:catalog`-twice idempotence run; browser-driven LTR + dark + RTL with page overflow measured at 0 on all seven routes, which caught two real defects (a lazily-cached fact registry nothing had fetched, so every product reported reading no lists; and an `effect` writing signals synchronously against my own comment claiming it did not). Backend 859 → 903 green (+44), admin 36 green, both locales build, and the untranslated-id set is byte-identical to baseline (405 unique, measured against a worktree of HEAD). **Accepted and stated:** compounds are now reachable only through the product — they are not deletable, so nothing is lost, but a direct URL is the only door if it is retired. **Found, not fixed:** `countReferences`'s `surrogate_fact` case counts only `strategy: 'fact:<key>'`, never a fact inside a `steps` array — so `compound_name` is hard-deletable today and would dangle `capByCompoundClass`. Pre-existing; this change adds a third uncounted surface.
  **Code review closed 14 findings on the above, four of them load-bearing.** (1) `setSurrogateProductIncomeRule` shipped with NO proof-change refusal, on a comment claiming it was "enforced one level down by `assertIncomeProofMatchesName`" — that function runs only when a BANK PROGRAM is saved, so nothing ran as a consequence of the product write and an operator could repoint five live programs with a 200. Now refused, reaching TWO hops (product → linked names → programs), with the misleading comment replaced by what was actually wrong. (2) The edit dialog wrote the link BEFORE the basis in every direction, but the server reads the STORED basis on an unlink — so a linked no-payslip name could never be switched back to payslip, on any retry. The order is now direction-aware: link-first when linking, basis-first when unlinking. (3) Linking a name that already held its own rule created the very fork the schema doc, `effectiveProgramNameRule` and the migration's RAISE all assert is unreachable. Fixed by REFUSING (`PROGRAM_NAME_HAS_OWN_RULE`) — the first attempt absorbed the rule instead, and browser-testing it against the live `doctor` row showed that trades a silent fork for an UNFIXABLE state: rule gone, unlink then blocked by `SURROGATE_PRODUCT_REQUIRED`, no way back. (4) `ParentClassBoardComponent` called `load()` from its constructor while reading signal inputs, which are not set until after construction — so `[childType]`/`[parentType]` were silently ignored and the board always fetched the compound defaults. Now an effect. Also: `SURROGATE_PRODUCT_IN_USE` interpolated a string ARRAY into a slot that reads as a count (`"compound_owner,car_owner اسم برنامج"`) — meta gains `count`; a missing product returned `PROGRAM_NAME_KEY_UNKNOWN`, which names the wrong object and sends the operator to the wrong screen (new `SURROGATE_PRODUCT_NOT_FOUND`, 404); the product list was 2N+1 serial round trips and `surrogateProductMeta` re-scanned the whole table for one boolean (now one batched read, `usedBy` grouped in a single query); `LookupValuesPanelComponent` re-fetched the expensive `GET /enumerations/types` aggregate PER PANEL and twice per write (now an input the host reads once); the board emitted nothing on a move, so a sibling panel showed stale class badges (new `changed` output); `lookup-value-list` rendered seven icons and registered NONE, relying on whichever host had patched them — order-dependent the moment a second host existed (it now owns them); the extraction left `rows`/`parentRows`/`loadingRows`/`NzModalService`/`RouterLink`/`NzButtonModule` and 13 icons dead on `lookups.page.ts`; a third `stableJson` was hand-rolled beside the shared one v18.1.7 centralised; `seed-collateral-products` omitted `active` from its product upsert, so a re-seed after a deactivate linked a name to an inactive product and reported success; and five i18n units orphaned by this change were left behind. **The products route moved to top-level `/surrogate-products`**: nested under `/program-catalog` it needed declaring before `:key` to avoid resolving as a catalog name, and no `routerLinkActive` config worked — prefix-matching lit both peer sidebar items, `exact: true` then left `/program-catalog/compound_owner` matching neither. A product is a peer of the catalog, not a child. 903 → 913 backend tests, 198 error codes in sync, LTR + dark + RTL re-verified with one active sidebar item per route and overflow 0.
- 2026-08-24 (v18.3.0): MINOR — **the compound-class board's tick is a real checkbox: unticking takes the compound out of its class and leaves it in none.** v18.2.0 modelled the class as mandatory, so a card already filed in the open class rendered as a checked, *disabled* checkbox — clicking it did nothing, which operators read as a broken control rather than as a rule. **One class max still holds:** `parentKey` is a single column, so the count is 0 or 1 and there has never been a path to two. What changes is that 0 is now reachable on purpose. The state itself is not new — the column is nullable and the board already had the whole vocabulary for it (a `No class` warn tag, unfiled-first ordering, a notice counting them) — what was missing was any API that could PRODUCE it, because `resolveParentKey` refused `undefined` and `''` for every type with a parent axis. **`null` on the wire, explicitly**, and it is the third value rather than a re-reading of either existing one: `''` is what v18.2.0 closed (stored, it passes the engine's `parentKey IS NOT NULL` filter, so the value LOOKS filed and prices nothing anyway — the same damage with none of the intent) and an absent field means "do not touch" on PATCH. So `resolveParentKey` gains a FOURTH answer behind an explicit `allowUnfiled`, passed by `setParentKeysBulk` and by nothing else: create still refuses (a value of a filed-under type is born filed) and a label-only patch still cannot reach it, so the one destructive spelling exists only on the endpoint built for moves and is audited per row in the same scalar `changes.parentKey {from,to}` shape — now with `to: null`. `ParentKeyMove.to` and the repo's `byTarget` map widen to `string | null`; the grouping still emits ONE `updateMany` for an unfile batch. **The consequence is accepted and stated, not hidden:** an unfiled compound is still a pickable answer (`enumOptions` filters `active: true` and nothing else), `factParentTable` answers `no_matching_row`, and that is not a skippable reason — only `rule_unconfigured` is — so the rule STOPS for every bank keying its cap table by the class. Verified end to end against the live seeded rows: EGB quotes `madinaty` at 4 000 000 and, with `dreamland` unfiled, returns `SURROGATE_NO_MATCHING_ROW` with no figures, while `check:collateral`'s registry invariant turns red naming the compound and exits 1 — that red line is the feature's safety net and is deliberately left as-is. The program is LISTED with its reason, never dropped. **A latent 500 fixed in the same edit, because `null` now means something:** `@IsOptional()` skips validation for `null` as well as `undefined`, so `parentKey: null` on create or patch slipped the pipe, reached `findByTypeAndKey(parentType, null)` and came back as an untyped `INTERNAL_ERROR`. Both DTOs move to `@ValidateIf(!== undefined)`; all four bad spellings (`null`/`''`/absent, on both endpoints) now return a typed 422 `VALIDATION_FAILED`, confirmed over HTTP. Unfiling a type with NO parent axis stays `ENUMERATION_PARENT_NOT_APPLICABLE` — refusing it is the point, and excusing `null` there would have quietly deleted that refusal. **Admin:** the filed card becomes a `<button>` (`aria-checked="true"`, no `aria-disabled`) with a new `unfile()` mirroring `move()` down to the optimistic write, the per-card `saving` flag, the rollback and the live region; the hover deepens the accent rather than tinting toward it, so the two directions do not read as one gesture. No confirm modal — the action is one click to undo and the panel's own warn notice fires immediately, which is louder than a dialog dismissed on the way through. The `aria-label` names the consequence, since a screen reader has no H1 in view to tell "untick" from "delete". Round-tripped in the browser: untick → the card moves to *Priced somewhere else* tagged `No class`, the notice appears, the rail count drops 4→3, a reload keeps it, re-ticking restores every one of those; a forced 500 rolls the card back with no notice. No schema, no migration, no new error code. 1 new i18n id, 1 dropped (`ccb.filed_lock`, added hours earlier to explain the lock), 2 reworded. Backend 849 → 859 green (+10, incl. the suite's first DTO-level `validateSync` cases pinning the three-value wire contract), admin 36 green, both locales build with the same 434 pre-existing warnings, LTR + RTL + dark verified with page overflow at 0.
- 2026-08-23 (v18.2.0): MINOR — **three compound classes instead of five, and a board that says where each compound is priced.** The five tiers (`cat_aa` · `cat_ab` · `cat_a` · `cat_b` · `cat_c`) read as an ordering nobody could infer — AA above AB above A is not a scale anyone recognises — and the two middle steps were never a decision an operator could defend on a screen. Now **Class A 6M · Class B 4M · Class C 2M** at EG Bank, with `A = Mivida · New Giza · SODIC East`, `B = Mountain View iCity · Palm Hills · Madinaty`, `C = Al Rehab · Dreamland · Another compound`. **The keys are NEW (`compound_class_*`) and that is the load-bearing half:** under the new scheme "A" is the TOP tier while `cat_a` was the MIDDLE one, so a recycled key leaves every half-applied state individually plausible and jointly wrong — re-file first and Mivida quotes 4M instead of 6M; rewrite the table first and Madinaty quotes 6M instead of 4M, silently, frozen onto `bank_offer.collateralCeilingEGP` where it cannot be corrected. Fresh keys make every half-state `no_matching_row`, which STOPS the rule and reports itself; a key is unrenameable by construction anyway, so reuse buys no fewer writes, only ambiguity. **One SQL migration** (`20260823130000_compound_classes_three`), not a seed run and not admin clicks: both halves live in Postgres (`platform_enumeration.parentKey` and `bank_program.incomeAssumption`) and are only correct together, `upsertLookups` is upsert-only so a dropped key outlives every database it reached, and a `compound_category` row can never be deleted through the API while deactivating one does NOT stop the engine reading it — there is no admin-only sequence that ends in a clean state. It creates the three classes, re-files by EXPLICIT compound key (never by old parent — that would silently re-tier a compound an operator filed themselves), folds every leftover (null / `''` / retired parent) to the lowest class, rewrites the stored cap table BY SHAPE so a second bank is carried and a hand-keyed table is left alone, re-roots the `team_estimated` marker paths (which embed the key, and whose collapsing pairs fold to one path — the right semantics, since either contributing figure being a guess makes the survivor one), deletes the five old rows, and then **RAISES rather than commits** if any compound is not under a live class or any cap table still names a retired one. **A board, because assigning was nine dialogs:** new `/lookups/compound-classes` — pick a class from the segmented rail, see every compound at once split into *priced here* and *priced somewhere else*, search, and move one or all-listed in a single transactional write. The tick is a **MOVE, not a checkbox**: a compound's class is a single parent and it must have one, so there is no unticked state to return to — a filed card is a checked, disabled checkbox and a candidate card names the class it would LEAVE, which is the half a plain checkbox cannot say. **Money is deliberately absent** (what a class pays is per BANK, so a figure would be a lie the moment a second bank configures the product); the board says the ORDER instead, as a rank accent derived by `color-mix` from `sortOrder` plus the tier in words — a scale you can see beats a number that is only sometimes true. Optimistic per-card write with rollback, unfiled-first ordering, a live region for every move, shape-matched skeleton, entry-only motion killed under `prefers-reduced-motion`. **New API** `POST admin/enumerations/parent-keys` — every id resolved and every target validated BEFORE anything is written, one deterministic-order `FOR UPDATE` prelude, one `updateMany` per target, and one audit event per row that actually MOVED, in the same scalar `changes.parentKey {from,to}` shape a single patch writes. **Three enforcement gaps closed with it:** `parentKey` was never validated against its parent type anywhere on the server (a typo saved 200 and surfaced as `no_matching_row` on a customer, which the income-rule validator explicitly declines to catch) — now one `resolveParentKey` allow-list decides it for create, patch and bulk; a compound could be created with NO class (`ENUMERATION_PARENT_REQUIRED`), and the DTO's `@Length(0,64)` let `''` through as a stored value that then passed the engine's `parentKey IS NOT NULL` filter; and a class could be retired out from under its compounds (`ENUMERATION_HAS_CHILDREN`, 409) while the engine kept pricing off it, because the parent walk filters the CHILD's active flag and never the parent's. A label-only patch on an already-unfiled row still saves — demanding a parent there would make the row unfixable by the very edit that fixes it. New server-side `PARENT_TYPE_BY_TYPE` / `parentTypeOf` / `childTypesOf` beside the four sibling axis constants (Principle IX: not `common/`). **Also:** a `parentKey` move now invalidates EVERY cached type, not just its own — `surrogate_fact` carries the derived `parentOptions` the bank's key-table editor is filled from, so the old class list survived for up to the TTL and read as "it did not work"; `countReferences` gains an explicit `DELETABLE_TYPES` gate served as `deletable` on the type summary, so the admin withholds a Delete button it knows will 422 instead of teaching the operator that Delete is broken; the value dialog's class becomes required and finally reads the shared `PARENT_TYPE_BY_TYPE` instead of a hardcoded ternary. 4 new error codes (193 in sync across backend + both dictionaries), 38 new i18n ids with ar targets, 1 orphan dropped. **Verification that would actually catch a botched migration**, which is what was missing: `check:collateral` gains one run PER CLASS at 40% down (the 20% baseline dies at the down-payment gate before the class table is read, and the one applicant it used is worth 2M under BOTH schemes — a migration that re-tiered everything wrong would have shipped green) plus two registry invariants; new `compound-class-registry.spec.ts` asserts the seed's own arrays — three classes, every compound filed, no class empty, no retired key recycled, and 6M > 4M > 2M by name. Backend 828 → 849 green (+21), 30 collateral assertions, both locales build with the same 434 pre-existing warnings.
- 2026-08-23 (v18.1.7): PATCH — **a code-review pass over the compound-owner product: five live defects in the rule engine and its two screens, plus the review's reuse and convention findings.** **(1) CAE refused every single-unit compound owner** — its `strongestUnitConfirmed` gate read `compound_best_unit_confirmed`, a question `enabledWhen`-gated on `compound_multi_unit = yes`, so a single-unit owner had no answer and got `fact_not_answered` from the bank whose whole policy is "one unit only". The question is now asked of every compound owner and carries an `I only own this one unit` option in the gate's allow-list — the same shape the two self-employed gates already use, because `enabledWhen` names ONE parent option and cannot express "or". `collateral-product-check.ts`'s baseline was masking it exactly, answering `multi_unit: no` AND `best_unit: yes` in one fact map, a combination the real branching cannot produce. **Requires `npm run seed:collateral`.** **(2) One skipped optional question killed all five compound quotes.** Steps ran eagerly, so `monthsOwned` — read only by two ownership-duration gates — demanded its answer even from the banks that turn on neither, and the unit price did the same at a bank whose ceiling comes from the unit TYPE. New pure `neededStepIds()` narrows evaluation to the answer plus the gates THIS BANK configured; statically derived, so it cannot depend on the values it decides to compute, and a bank that does read a figure still demands it. **(3) `coalesce_empty` was unreachable for the one product it was written for** — the compound frame's first candidate is a `pickByFact`, which needs no figures of its own, so `isStepConfigured` answered `true` unconditionally and a bank could save with no derivation at all and quote nothing, live and silent. The validator now recurses through picks and coalesces. **(4) `withStoredStructure` discarded gate-only and output-only writes** (it guarded on `steps` while the overlay replaces all three) and **dropped the name's policy fields** — `dbrCapPercentOverride` / `requiredDocuments` / `combinationRule`, which no pipeline screen edits — so the first successful Save lost them for good. Both now carry, under the same rule as the structure: only when the incoming write states none of them. **(5) The catalog Save deleted every `team_estimated` marker on the name.** The screen posts no markers, the service read `?? {}` as "the full set is empty" and replaced wholesale. Absent now means "not touching them" (stored set carried forward, pruned to paths the new rule still has); an explicit `{}` still clears. **Also fixed from the review:** `numberByKey` gate tables were the one place a wrong key was discovered on a customer instead of at save — now checked against the keyed fact's question options, like every choice step; FABMISR's down-payment gate was provably dead (its band table opened at the same 250 000, and steps run before gates, so a short applicant died at `no_matching_band` instead of hearing `DOWN_PAYMENT_BELOW_MIN`); `figuresChanged` compared two differently-key-ordered blobs, so every no-op catalog save logged a change (shared `stableJson()`, with `seed-program-catalog`'s local copy deleted). **Admin:** the empty-key-list state no longer hides a table's stored figures; a second-column key the primary lacks is visible, editable and removable instead of invisible-but-posted, and the second column is validated at all; `branchLabel` indexes the RAW `of` list (a literal member used to shift every column heading) and renders the Arabic label in the Arabic build; two `keyTable` columns merge only when they share an op AND a fact; `'none'` no longer coerces to `'scalar'`, so a derived step says so instead of offering a money box the engine never reads; `activeDerivation` scans alternatives only, not the adjustment coalesces; the segmented rail's `aria-controls` points at a real `role="tabpanel"`; the rule's Save and its refusal text now travel with the operator instead of vanishing when they leave step 1. **Reuse / perf / conventions:** one `factKeysReadBy()` replaces the fact walk's second copy; the fact option lists are memoised so a keystroke no longer hands every child a fresh array; `secondValue` is a map, not a linear find; one `<input>` in `figure-field` instead of two branches, the second of which bypassed `ngModel` through an `$any` cast (A15) — grouping is now the money directive's own flag, with its pure half split into `money-format.ts` and unit-tested; the hand-rolled applies toggle becomes `nz-switch`; five raw `10px` field radii and the 44px field height take tokens; `styles.scss`'s addon corners and the rule editor's row indent become logical properties (A19). New `--size-field`. 4 new i18n ids with ar targets. No schema, no migration, no new error code. Backend 822 → 828 green (+11 specs), admin 31 → 36, both locales build with the same 434 pre-existing warnings, zero lint findings on changed lines. **Deliberately not done:** `pickByFact` is N-ary but the merged editor stays two-column (three or more render as stacked editors, correctly labelled) — an N-column table would fork the key-matching, re-key, reorder and delete paths per column on the one control that edits live figures; and `groups()` still rebuilds per keystroke, which is now cheap because the child inputs are stable.
- 2026-08-23 (v18.1.6): PATCH — **"The whole calculation, step by step" numbers its lines in a grid column and says what each one reads.** Two problems, one of them only visible in English. **(1) The alignment:** the list was an `<ol>` with `padding-inline-start: var(--space-5)` (24px) and default outside markers, which CSS lays out to the LEFT of the content box — so `20.` hung further out than `1.`, the digits ran ragged, and the wide ones sat left of the summary that labels them. Arabic-Indic numerals are narrower, which is why it read as an English-only defect. Now a two-column grid per line (`2ch` + `minmax(0,1fr)`, tabular figures, `text-align: end`): 1, 10 and 20 occupy the *same box* and every title starts on the same pixel, measured identically in LTR and RTL. **(2) The list said the same thing repeatedly:** twenty rows drawn from five op titles — "A table of ranges" three times, "A percentage of an earlier figure" four, and rows 5 and 6 the same eight words back to back — so the only place the product reads as ONE calculation could not tell its own lines apart. `flow` returns `FlowLine[]` instead of `string[]`, and each line now carries what the RULE already knew and the list was throwing away: the ordinals it reads (`↑ 5 6`, derived from `stepRefs`, which is what separates rows 9 and 10 from each other and row 13 from rows 15 and 18), which column of a `pickByFact` it fills (`· Everyone else` / `· Already banks here`, from the existing `branchLabel` — the pick is the only place the rule says which of two identically-keyed tables is which), and whether it is the answer (`output.from`). A step read only by a gate is tagged as such, because `requiredDpPct` sits past the answer and otherwise looks like a step the calculation forgot. Nothing is stored and nothing is keyed by step id: the same derivation runs on `car_owner`, where the whole product is three lines. **Polish with it:** the summary gains a hover surface, a focus-visible ring and a count, so the closed state distinguishes a 20-step product from a 3-step one; the list is set off by a start-side hairline rather than a box (this panel already sits in a card); the open transition reuses the existing `rule-row-in` keyframe and is killed under `prefers-reduced-motion`. **Contrast:** the ref and tag chips are inked `--color-text-secondary`, not the tertiary the line runs at — a chip darkens the ground under its own text and tertiary lands at 3.5:1 — and the answer pill takes `--color-text-primary` on the accent tint, since accent-on-accent-subtle is 3.3:1 in light mode and the tint plus the weight already single the line out. 3 new i18n ids with ar targets, none reworded, none dropped. No schema, no API, no error code; 31 admin tests green, both locales build, verified against the live seeded compound and car names in LTR, RTL and dark with page overflow measured at 0.
- 2026-08-23 (v18.1.5): PATCH — **the group rail is one segmented control, and a figure carries its unit inside the field.** Two things on step 1 read as unfinished. **(1) The rail** drew three free-standing pills, each with the group's row TOTAL on its trailing edge *and* a note underneath that already contained it — so "Ways to work the figure out **4** / 2 of 4 set" said the total twice and the trailing number read as a stray figure spliced onto the title. The `count` is gone (the note is the count, in words), and `app-rail-tabs` gains an opt-in `appearance="segmented"`: one inset track, one raised live segment, so three peer groups read as ONE control with one thing on stage rather than three unrelated buttons. Opt-in, not a repaint: the category board's rail carries four accents and a wash reads as an identity, which is what a track would flatten. The track is `--color-surface-page` and the segment `--color-surface-default` because that pair is directional in BOTH themes (light #F8F6F4 → #FDFCFB, dark #0A0710 → #15101C) — `--bg-subtle`/`--bg-muted` INVERT in dark and would have sunk the segment that is supposed to be lifted; the accent-mixed hairline is load-bearing for the same reason, since `--shadow-sm` is a black rgba and casts nothing on near-black. **(2) The figure input** was a bare 12rem slab with the unit, when there was one, floating outside it as a prefix — and on a gate there was no unit at all, so "At least [    ]" was the same box whether the bank meant twenty per cent of the price or twenty pounds. New `figure-field.component.ts` (the markup was about to be its third copy) makes it one control: the shell owns the border, the hover and the focus ring, the unit is an affix INSIDE it, and the figure is end-aligned against that unit so a grouped amount grows away from the word that says what it measures. A gate's unit is read off the step it COMPARES, through the `valueKinds` map the value-column headers already use — so a down-payment SHARE gate says `%` and a down-payment AMOUNT gate says nothing, deliberately: nothing on this screen proves `monthsOwned` is months rather than pounds, and a confidently wrong `EGP` on a contract-age gate is worse than a blank. **Two defects fixed with it:** these were the only figure inputs in the rule editor with no `appMoneyInput` (A27) — a seven-digit floor rendered as `2000000` while the table cell one row below grouped its thousands — now applied wherever the kind is not provably a percentage or a multiplier (grouping a two-digit month count changes nothing, and the payload still carries the raw string, verified); and the field is 9rem for a percentage instead of a 13rem money box around "20". A11y: the row body became a `role="group"` named by the row, because ten conditions each offering "At least" are ten identical fields to a screen reader in forms mode, and the unit now joins the field's accessible name. New `--radius-field: 10px` — the universal field radius DESIGN_SYSTEM.md already documents was a raw literal in eight places in `styles.scss`, which is why a component reaching for `--radius-lg` sat 2px rounder than the input in the next cell. **No new i18n ids** (the two bound labels keep theirs, moved to `$localize` so the field can render a real `for`-associated label), no schema, no API, no error code; both locales build with the same 434 pre-existing missing-translation warnings, 31 admin tests green, and the screen was verified against the live seeded compound name in LTR, RTL and dark with page overflow measured at 0 and the save payload confirmed to carry `"minValue":"2000000"`.
- 2026-08-23 (v18.1.4): PATCH — **step 1 of the program-name screen fits a screen, and its Save works for the first time.** The income step was 2 100px on the compound name — a rail, two ledes, thirteen answer chips over three rows, and then all three groups (4 derivations + 2 adjustments + 10 conditions) stacked with one alternative expanded into TWO key-table editors. Asked for an inner stepper and **argued against one, then built the thing it was reaching for:** a stepper claims an ORDER these groups do not have (fill exactly one of the four derivations, then optionally an adjustment, then optionally a condition — an operator usually touches one row and leaves), a nested rail doubles "where am I?" and puts two Back/Next pairs a few hundred pixels apart, and the group count is DERIVED, so `car_owner` would get a one-step stepper apologising for itself. Instead, three changes, and step 1 goes 2 100px → **1 119px** (`car_owner`, one group, 748px). **(1)** The groups move onto the SHARED `app-rail-tabs` — one group on stage, every group's count visible at once (which a stepper hides), no order implied, and no rail at all below two groups. **(2)** A `pickByFact` pair of KEY TABLES becomes ONE table with two value columns (`KEY · Everyone else · Already banks here`), because the two steps are keyed by the same fact by construction — the same table read for two kinds of customer. As two stacked editors they cost 550px for six numbers and put the two figures being compared out of sight of each other; side by side, 2 000 000 → 3 000 000 is the row. New optional `secondRows` / `secondLabel` on `app-income-key-table` (`null` = every other caller, unchanged), matched **by key, never by index**, with delete-on-empty so clearing the column leaves it genuinely unconfigured — which is how a bank declines it — and re-key / remove / reorder carrying both columns so the two can never disagree. A pair of BAND tables still renders as two editors: there is no single row two ranges share. **(3)** The answer chips cap at one line with a `+N more`. **The blocker found by looking at the error instead of the screenshot:** saving step 1 has NEVER worked for a pipeline product — `422 PRODUCT_RULE_INVALID / no_steps`, every time, on every collateral name. The screen deliberately posts figures only (`ruleFromForm` omits `steps`/`gates`/`output`, because re-posting a copy it merely rendered would let a stale screen replace the product), and `validateProductRule` holds a `steps` rule to having steps. Closed with new pure `withStoredStructure` in `income-rule-inherit.ts` — the module that already owns "the structure is the catalog's" — which overlays the STORED structure only when the incoming rule states none, so the seed and the API can still change a product's shape while the screen cannot. Not a second copy of `mergeProductRuleStructure`: it reuses it and adds only the *when* (A25). +4 backend specs pinning both directions and the two no-ops; 812 → 816 green, 23 `check:collateral` assertions still exact, admin 31 green, both locales build. Verified end to end against the live seeded name: the rail walked, a second-column figure typed → dirty → discarded by reload → typed again → **saved 200 → persisted → restored**. 4 new i18n ids with ar targets, 1 reworded (the column header, which wrapped).
- 2026-08-23 (v18.1.3): PATCH — **`/program-catalog/:key` is three steps, and the loan-type assignment is ONE screen instead of four tab clicks.** The page stacked three unlike things on one scroll: an income-rule card (per NAME), a loan-type tab rail, and behind each tab a switch (per TYPE) plus a forty-question board (per TYPE). Two of those are per loan type and one is not, so the page asked the operator to keep track of which as they scrolled — and the assignment they came for, "which types is this name sold under?", could only be read one tab at a time with the other three held in memory. Now: **① How the income is worked out · ② Where it is offered · ③ What it scores on**, on the SHARED `app-wizard-steps` rail (the bank-program wizard's own), so a step here reads exactly like a step there. **Non-linear by construction** — a settings screen, not a creation flow: every step is reachable at any time, each saves on its own terms (ticks autosave, the rule keeps its explicit Save), nothing is submitted at the end and there is no Finish. Step index lives in the URL as `?step=`, beside the existing `?loan=`, so a pasted link and a reload both land where the operator was. **Step 2 is the real change:** the per-tab gate is GONE and all four loan types render as switches at once in a 2-up grid, `toggleOffered(category)` taking the row it toggles instead of reading the open tab. Its titles are the loan TYPE alone — four rows each opening with the program name repeated it four times under an H1 that already says it, and pushed every title to two lines — with the full sentence kept on `aria-label`, where a screen reader has no H1 in view. `offeredCount() === 0` states "no bank can offer this name yet" as a status, not a validation error: the row is saved and legal, it is the name that is unsellable. Rail status is derived and only step 2 can be WRONG — step 1 is legitimately blank (a payslip product states no rule) and step 3 is advisory by design (`saveWeights` never reads it), so neither ever asks for attention. **Copy fixed while moving it:** three gate hints interpolated the category straight after an article ("as a Auto Loan") — unfixable in one string for every locale — so they now say "a program of this type" / "here", which the ROW already establishes; the locked panel said "turn on the switch above" about a switch that is now a step away, and says which step and offers the trip instead; `pnd.rule_sub` is deleted because the rail's caption says it in fewer words one line up and the pipeline's own closing note says the other half at the foot of the same step. i18n: 13 new ids with ar targets, 5 rewritten (three had their placeholder set CHANGED, which fails the whole ar build as a mismatch, so they had to move together), 2 orphans dropped. Verified against the live seeded name: all three stages, the rail, the captions and the nav in LTR and RTL with page overflow measured at 0, and one offer switch toggled → persisted through a reload → toggled back and re-read equal to its starting state. No schema, no API, no error code; 31 admin tests green, both locales build, and the file's 20 pre-existing lint errors are down to the one repo-wide `*Page` class-suffix convention.
- 2026-08-23 (v18.1.2): PATCH — **"How the income is worked out" is grouped by what the operator has to DECIDE, and the section is styled by tokens that exist.** The compound rule rendered as one numbered ladder of THIRTY equal rows — eleven of them arithmetic with nothing to type, four of them alternative ways to reach the same figure of which each bank fills exactly one, a step that exists only to be compared against sitting a dozen rows from the gate that reads it, four gates carrying the same reason code and therefore the same title ("Minimum the customer must have paid", ×4), and eight repetitions of the same empty-table paragraph behind eight `nzType="primary"` buttons. **First cause, and the reason it read as unstyled rather than badly styled:** `product-rule-editor.component.ts` reached for six tokens the theme does not define — `--surface-sunken`, `--surface-raised`, `--font-size-sm`, `--font-size-xs`, `--line-height-relaxed`, `--color-warning-text` — so the step discs had no disc, the tags had no chip, every hint rendered at body size and the warn tag had no ink (A18); `program-name-detail.page.ts` reached for two of the same, which left the refusal notice with no surface and made the loading skeleton's `linear-gradient` INVALID at computed-value time, i.e. four invisible boxes where a shimmer was supposed to be. All eight were only ever used in those two files. **Second, the structure, all of it derived from the rule and none of it stored or keyed by step id:** a `coalesce` of steps only names ALTERNATIVES (fill exactly one), a `coalesce` that also offers a `{const}` names ADJUSTMENTS (blank is a real answer — the fallback applies), a gate's `right` step is a CONDITION's own figure and now renders on that gate rather than as a far-away orphan, everything else that takes figures is the CHAIN, and the arithmetic falls out of the actionable list into a closed `<details>` where it still reads as the whole calculation. 16 rows instead of 30, and only blank-and-optional ones fold — a figure a program still OWES never folds away. A `pickByFact` is titled by the column it picks for everyone (its two "Whichever column fits the customer: whether they already bank here" rows were the same words twice) with the pick as a qualifier, and its columns render as two labelled slots. Gates get a structural qualifier — `numberByKey` → "one figure per Employment status", a right-hand step → "compared with Unit contract price", otherwise "measured on Share of price paid" — which is what tells the four down-payment gates apart. **Third, the figures say what they ARE:** "Assumed monthly income (EGP)" is the eleven single-fact methods' column and on a pipeline it is simply false (the car table holds 70/60/50, the multi-unit table 100/95, the compound cap table millions). A figure's kind is stored nowhere but implied exactly by the arithmetic that consumes it — the SECOND input of a `percentOf` is a percentage, of a `multiply` a multiplier, everything else money, and a `coalesce`/`pickByFact` passes the consumer's answer through to its members; a gate's right-hand table inherits its left's kind, which is how a required-down-payment BAND table is known to hold percentages when nothing multiplies by it. New optional `valueLabel` on `app-income-key-table` + `app-income-bands-editor` (`null` keeps the old column, so nothing but a pipeline moves); a gate's table says "The requirement". **Two real defects fixed with it, both the same root cause as the blank picker:** (1) `steps` is deliberately absent from `incomeMethodGroups()` — a pipeline is not a twelfth method — so the catalog's Method select rendered a BLANK trigger over a live product, and every option in it was a one-click way to replace the product's structure with a single-figure table (`ruleFromForm` posts whatever the control holds). The picker is now hidden for a pipeline and the state named in a line instead. (2) `incomeMethodLabel` had no `steps` case and returned `undefined`; three call sites patched around that individually and a FOURTH did not — `catalogProof()` on the wizard read the empty label as "this name states no income proof", put the whole income step behind a "Nobody has said what … reads its income from" notice and left the product-rule editor **unreachable for all seven collateral programs**. Answered once in `incomeMethodLabel`; the detail page's local copy deleted. **Also:** the seed buttons drop from `primary` to `default` (a seed button is not a page's primary action, and a product rule renders one per unfilled table) and the empty state is capped at 46rem (`space-between` across a full-width block threw the button half a screen from the sentence explaining it); the read-only detail page's chips row was clipped by its own card — two `white-space` declarations in one rule and `nowrap` won — and now wraps in a row that stacks to the card's full width, with page overflow measured at 0. **Arabic:** `messages.ar-EG.xlf` carried two placeholder-name mismatches (`bank_programs.step.caption`, `bank_programs.income.src_catalog`) that failed the ENTIRE `development-ar` build on HEAD, so no RTL check was possible at all; fixed, then LTR and RTL both verified against the live seeded rows. 26 new ids with ar targets, 4 orphans dropped (`bank_programs.income.steps_title` — the pipeline no longer needs a second heading over it — plus three hints describing a layout that is gone). No schema, no API, no error code, no spec change; 31 admin tests green, both locales build. **Not done, stated:** the detail page's pipeline chips still name raw step ids and raw option codes (`dpAmountByEmployment · government_employee → 0.00`) — naming them needs the fact registry and the step structure on a page that fetches neither.
- 2026-08-23 (v18.1.1): PATCH — **the collateral DEMO is two products, and the retired one is deleted rather than left live.** The bank-program wizard's income-method picker lists every active `surrogate_fact` row, and `seed-collateral-products.ts` seeded fourteen of them for two products, so the list read as more product than the demo is. The **club-membership loan is gone** — its catalog name, its two bank programs (`ABK-CLUB-MEMBERSHIP` / `CIB-CLUB-MEMBERSHIP`), its two questions, its group, its fact and its three `club_class` lookup rows. Dropping them from the seed arrays only stops them being WRITTEN, so a new `pruneRetiredDemoProducts()` states the retirement by explicit key and DELETES, in FK order (`application_answer.questionId` is `onDelete: Restrict`, so stored answers go first; everything else cascades). Past `bank_offer` rows and archived snapshots stay — they hold their own copy and reference the program by code, not by FK (Principle I / A6). **In its place, a loan against a car the applicant already owns:** one catalog name `car_owner` (`car` only), three questions in ONE group (`owns_car` required + `owned_car_value` / `owned_car_age`, both gated on it — the compound pack splits gate from pack because two categories share its gates group, and CAR shares neither), two facts, and a three-step rule reusing `factNumber` → `factChoiceTable` → `percentOf`. **No new op, no new gate:** a value floor or an age cap would each cost a `GATE_REASON_CODES` value across five surfaces (A25), and "an older car borrows a smaller share" says the same thing in figures the bank already states. ABK states its own shares (75/65/50), CIB inherits the catalog's (70/60/50) — deliberately different, so a run reading the wrong side shows up as a wrong number. `club_class` leaves the enumeration-type union on both surfaces, the Manage-values rail and both its i18n ids. `check:collateral` still runs 23 assertions, two of them now the car programs (ABK 800 000 × 65% = 520 000, CIB × 60% = 480 000); backend 812 green, no spec added or removed.
- 2026-08-22 (v18.1.0): MINOR — **the second column: a bank may lend more to a customer it already has, and the applicant is who says so.** v18.0.0 seeded only the new-to-bank column of ABK's and FABMISR's cap tables, so both programs' own `maxAmountEGP` (4.5M / 2M) was unreachable, and the source design's top-up / X-SELL columns were recorded as an accepted gap for want of a CRM flag. Closed by ASKING: one bank-agnostic `existing_bank_relationships` MULTI_SELECT question (options from the `bank` registry via the EXISTING `optionsFromBanks`), and a per-program DERIVED fact. New pure `matching/pipeline/bank-relationship.ts` (`bankSlug` + `bankRelationshipFact`); `bankOptions` in `seed-questionnaire.ts` now calls the same `bankSlug`, so the question's option codes and `BankProgramSnapshot.bankName` are ONE function rather than two that happen to agree. New `DERIVED_FACT_KEYS` beside `surrogate-fact-registry.ts`: a derived fact has no registry row and no bound question (its question is multi-pick, deliberately unbindable), so `validateProductRule` accepts it without one, `surrogateFactsFromAnswers` REFUSES to emit it (an operator registering a row under the same key must not let a customer answer overwrite a computed value), and its branch codes are validated against the engine's own closed list. 14th op `pickByFact` (`{fact, branches[], of[]}` — `branches` is STRUCTURE, the catalog's, positional against `of`): picks the column the answer names, and falls back to the FIRST CONFIGURED column both when the bank sells no second one and when the optional question was skipped — never `fact_not_answered`, because an unanswered segment is not a missing requirement. Two new `PRODUCT_RULE_INVALID` reasons (`branches_empty`, `branches_mismatch`). Seeded: ABK 3M/3.5M/4.5M top-up, FABMISR 1.25M/1.5M/1.75M/2M X-SELL. The X-SELL TENOR exception stays out and is re-stated as the gap it is — the tenor cap comes from the cascade, whose `ApplicantContext` carries no facts.
  **DBR by employment**, the other v18.0.0 gap, is also closed: `dbrCapPercentByEmploymentType` on `DbrSetting`/`EligibilityConfig`, resolved in `resolveDbrCap` AFTER the rule override and BEFORE the scalar and the income bands (a per-employment cap is a statement about the person, not about what they earn), folded through `coarseEmploymentType` so a bank never writes the questionnaire's vocabulary. CAE seeds `{self_employed: '40'}` and the 40 ÷ 50 baseline haircut then falls out with no third setting — `check:collateral` pins it: the ceiling stays 300 000 and the amount written drops to exactly 240 000. **Three latent defects in the same area, fixed with it:** `normalizeEligibility` dropped `ageMinSelfEmployed`/`ageMaxSelfEmployed`/`minMonthlyIncomeSelfEmployedEGP` entirely (so ABK's and CAE's 25–65 never applied), `COARSE_BY_DETAILED` had no row for the questionnaire's real codes `private_sector_employee` / `business_owner_company_owner` (so `isSelfEmployedBucket` was false for every real business owner and every `selfEmployed*` setting on every program was dead), and `quote.ts`'s maturity clamp read the generic `maxAge` while `checkEligibility` read the self-employed one.
  **The extract's self-employed gate**, third gap: two new optional questions (`self_employed_licence`, `business_years`) whose third option is "I'm not self-employed" — `enabledWhen` carries exactly ONE option code and "shown to a business owner OR a freelancer" is two, so the answer set absorbs what visibility cannot express — plus two `choice` gates using `expect` as an ALLOW-LIST (CAE turns both on). New gate reason codes `SELF_EMPLOYED_DOCS_MISSING` + `BUSINESS_TOO_NEW` across all five surfaces in one change (A25): `GATE_REASON_CODES`, `error-codes.ts` (200), both `error-codes.*.json`, the admin's `GATE_REASON_CODES` + `gateTitleFor`, and the Flutter `GateReasonCodes` + both ARBs.
  **Catalog DEFAULTS widened, not the mechanism** — `seedFromCatalog` / `detachFromCatalog` already did exactly what "catalog default, bank overrides" means, `stepParams` included. What was missing was breadth: the catalog rule shipped ONE default table. Now it also ships the top-up column and the paid-capital scalar. Still undefaulted, each for a stated reason: every GATE (a gate default is LIVE for a bank on `amounts: 'catalog'` — it would hand an inheriting bank a refusal rule it never chose), `capByCompoundClass` and the BAND tables (a miss there is `no_matching_row`/`no_matching_band`, which STOPS the rule rather than skipping the step, so one classless compound or one below-floor down payment would kill a quote another derivation could have priced), and the neutral `multiUnitFactor`/`jointFactor` tables (all-100 changes no figure and still turns an optional answer into a requirement, because a CONFIGURED table reads its fact).
  **`collateralCeilingEGP` now leaves the backend** (it was write-only): on the apply offer projection and on `PreviewFigures`, then through the Flutter model → entity → results VM → a line on the priced card ("your unit supports up to X") beside — not instead of — the affordability ceiling, which answers a different question. `missingFactKeys` stays plumbed-but-unrendered, deliberately.
  **`BELOW_PROGRAM_MIN_AMOUNT` is now checked before affordability**, not only inside the DBR shrink loop: a request or a ceiling below `minAmountEGP` was quoted anyway. It moved 8 baseline rows from `OBLIGATIONS_EXCEED_ALLOWANCE` — all of them a 150 000 ask against a 300 000 mortgage floor, i.e. the old reason blamed the applicant's debts for a loan the bank does not write at any obligation level. `baseline-offers.ts` gains a FIELD-level intended-reason allow-list rather than waiving those programs wholesale. **Admin:** the read-only program detail page rendered a blank Method and "Not entered — this program quotes nothing" for every configured collateral program (`incomeMethodLabel` has no `'steps'` case, `incomeRows` read only `keyTable`/`bands`) — both fixed, plus the same root cause on the wizard's review row; `factsReadBy` and the check panel now walk `gate.right`; Manage values can finally SET and SHOW `parentKey` (an operator-added compound was born classless, and `factParentTable` quotes nothing for a classless value). The club pack leaves the `mortgage` assignment (the `club_member` name is `personal`-only, so its extra step was unreachable product). 15 new i18n ids with ar targets. **Tests:** +26 backend specs (`pickByFact`, `bank-relationship`, `dbr-cap-by-employment`, gate.right), the first Flutter test for the gate-reason mapper (11, both locales — the file's own comment had asked for it), and `check:collateral` grows from 17 to 24 assertions including an X-SELL pass per bank and NBE proving the catalog's top-up DEFAULT. `surrogate-binding-codes.spec.ts` matched `const BUSINESS` as a PREFIX and broke on a new question const — now matches `const NAME:`. Backend 787 → 813 green.
- 2026-08-19 (v18.0.0): MAJOR — **a rule may output a borrowing CEILING instead of an income: collateral products (compound ownership, club membership) are DATA.** Every income method until now read ONE fact and looked it up in ONE table. The compound-ownership guarantee reads ten answers, derives a property ceiling rather than an income, carries product gates (down-payment floor, unit-price floor by contract year, contract-age window, multi-unit block, joint-ownership haircut), and is sold by four banks who derive that ceiling from four *different* things. New strategy token `'steps'` (in `matching/types.ts`, NOT in `INCOME_ASSUMPTION_STRATEGIES` — that set is the closed list of built-in methods and a pipeline is not a twelfth one). New pure `matching/pipeline/product-rule.ts`: 13 arithmetic-only ops in the codebase's **first name→fn registry** (`constant` · `factNumber` · `factChoiceTable` · `factParentTable` · `bandTable` · `percentOf` · `upliftPercent` · `multiply` · `sum` · `subtract` · `minOf` · `maxOf` · `coalesce`), declarative gates, and `factsReadBy` / `optionalStepIds` / `isStepConfigured` derived from the rule, never stored. **The catalog name owns the STRUCTURE (`steps`/`gates`/`output`), the bank owns only `stepParams`** — `effectiveIncomeRule` merges the structure on every read even when `amounts: 'own'`, and `stripCatalogStructure` keeps a bank row from storing a copy that would go on running yesterday's pipeline. **`coalesce` is what makes one frame serve four derivations:** the catalog offers all four cap steps, each bank fills exactly one, and only `rule_unconfigured` ("this bank stated nothing") is skippable — an unanswered fact, a key with no row, a value outside every band all still stop the rule with their own reason, and every op now checks its own configuration BEFORE reading any fact so a declined derivation never demands an answer. Gates work the same way: a gate applies only when the bank turned it on (a bound, a table, or `applies: true`), the same posture `checkEligibility`'s allow-lists already take. Money: new `matching/pipeline/product-rule-ceiling.ts` converts ceiling → the income it implies, in `quoteProgram` step 3b and nowhere else (the first point where the cascade rate AND the final tenor both exist); `monthlyInstallmentRaw` added to `pmt.ts` so the annuity round trip is LOSSLESS — with no obligations and cap == baseline the affordability ceiling comes back EXACTLY equal to the ceiling, and the source design's `dbrRatio` falls out as `applicable ÷ baseline` with no setting of its own. `programMax` is clamped to the ceiling (`binding: 'collateral_ceiling'`). One additive migration `20260819090000_offer_collateral_ceiling` freezes `bank_offer.collateralCeilingEGP` (Principle I / A6); `IncomeOrigin` gains `'ceiling'`, which **never meets a declared salary** — a payslip must not rescue a product whose collateral the bank has not priced. Validation: `validateProductRule` reuses `validateKeyTable`/`validateBands` per step and adds `PRODUCT_RULE_INVALID` (one code, 17 `meta.reason` values, both locales) — plus `IncomeRuleValidationOptions.figuresRequired`, `false` for a CATALOG write, because a catalog rule's figures are its banks' to fill and holding it to a bank's completeness would make the compound frame unsavable. Refusals are 200-body reasons, never filters (A33): `PRODUCT_RULE_GATE_FAILED` + the closed 7-value `GATE_REASON_CODES`, all 8 declared in `error-codes.ts` and translated in both dictionaries, carried through apply AND preview with `missingFactKeys` so a card can say "answer these six questions" instead of showing a blank. Value-source markers needed NO walker change — `markablePaths` already recurses `stepParams` by key. **Questions: gate in the funnel, pack on tap.** One required yes/no per product (`owns_compound_unit`, `has_club_membership`) and the heavy questions as their own `QuestionGroup` — one mobile step, every question optional and `enabledWhen`-gated, so a non-owner never sees it and ten products do not add ten steps. All 12 pack questions are `surrogate_fact` rows bound to them, so answers reach the engine with **zero client code**. New lookup types `compound` (each row filed under its class via `parentKey`) / `compound_category` / `club_class`, which is what lets a bank key a 5-row table while the customer picks one of hundreds by name — replacing the source prototype's client-side substring match against a hardcoded high-end list (its bug #2). New `npm run seed:collateral` writes lookups → questionnaire → facts → 2 catalog rules → 5 bank programs (ABK/EGBank/FABMISR/CAE compound + ABK club) and validates every rule through the admin's own path; `seed-banks` gains EG Bank, FABMISR, Crédit Agricole Egypt. New read-only `scripts/collateral-product-check.ts` quotes the REAL seeded rows and asserts §10's ceilings — because a rule can be valid and a program live while the string join between step ids and `stepParams` keys silently fails. Admin: `product-rule-editor.component.ts` renders the pipeline as a numbered summary and, on the program variant, each step's figures through the EXISTING key-table / bands / scalar editors (`STEP_OP_SHAPE`, so a new op that reuses a shape costs no UI); names the ACTIVE derivation, tags declined steps "Not used by this bank"; the check panel gains one sample input per fact the pipeline reads. 44 new i18n ids with ar targets. Flutter: gate-reason mapper + 9 ARB strings in both locales, and a "Skip for now" CTA on an all-optional step. **Six of the source design's listed bugs die by construction** (#1 tier fall-through → a below-floor value is `no_matching_band`, #2 substring high-end match, #3 two diverged engine copies, #5 unserializable JS predicate → declarative gates, #6 `Infinity` in JSON → `toExclusive: null`, #10 float money). Tests 772 → all green, INCLUDING the 15 `MatchingPreviewService` ctor-arity failures that predate this work. **Accepted gaps, stated not faked:** FABMISR's X-SELL column needs a CRM flag the platform has no source for; CAE's 50/40 DBR split by EMPLOYMENT is not expressible (`dbrBands` key off income) so it is seeded at its baseline; the catalog variant does not yet AUTHOR a pipeline — a new product is still an API or seed action, while configuring a bank on an existing one is fully admin.
- 2026-08-17 (v17.0.0): MAJOR — **a catalog program name states exactly ONE income proof; every surrogate program under it reads that one, and a bank changes only the AMOUNTS.** PR 1 (the five applied `20260817090*` migrations) had already moved the rule onto `platform_enumeration.incomeRule` write-only; this is PR 2 — it READS it, enforces it, and rebuilds both screens around it. **The eleven-item Method dropdown is DELETED from the bank wizard's step 5**: the name already decided, so the step asks one thing — *catalog amounts, or this bank's own*. **No nullable column, no new table, no sentinel strategy**: one optional field, `amounts?: 'catalog' | 'own'`, on `IncomeAssumptionConfig`. ABSENT reads as `'own'` because every stored row predates it and carries its own numbers — a default of `'catalog'` would re-point the whole book at tables it has never quoted from. New pure `matching/pipeline/income-rule-inherit.ts` (`inheritsCatalogAmounts` / `effectiveIncomeRule` / `stripInheritedAmounts`); the merge runs in `toBankProgramSnapshot`, already the single Prisma-row → engine-snapshot mapping, so the resolver still receives one finished rule and cannot tell an inherited table from an authored one. Its five callers pass a new uncached `programNameIncomeRules()` (apply, preview, calculator — which gains the enum repo — and the check overlay, which merges into the DRAFT because the snapshot's copy is discarded). `persistableIncomeAssumption` strips the figures on `'catalog'`, or the link would be a one-time copy wearing a link's label. Save enforcement (`income_surrogate` ONLY — a payslip program consults no proof, and `doctor` legitimately carries both kinds): `PROGRAM_NAME_INCOME_PROOF_MISMATCH`, `PROGRAM_NAME_INCOME_PROOF_MISSING`, `INCOME_PROOF_IN_USE` (422, ar+en), all save-time, never read-time, and grandfathered on an unchanged (name, proof) pair so a legacy program is not frozen out of the edit that would fix it. New `GET`/`PUT admin/bank-programs/program-names/:key/income-rule` — on THAT controller, not `admin/enumerations`, because every piece that decides whether a rule is acceptable already lives there and the reverse import would make the two modules circular; the audit event is still `PLATFORM_ENUMERATION_UPDATED`, keyed `incomeRule` and recording the STRATEGY move. `catalogIncomeRulePaths` (shared walk, `incomeRule.` root) so a marker means the same figure on both screens. Reconciliation: `scripts/income-proof-conflicts.ts` (SPLIT / MISMATCH / MISSING / UNFILED, exit 1 when dirty) found five real problems — `20260817120000` states `declared` on `professional` / `equipment_finance` / `pharmacy` / `working_capital` and `byYearsInPractice` on `doctor` (ABK's table; both its programs keep their own, so no quote moves) and clears an orphan marker `090400` left on a NULL rule; `seed-surrogate-demo.ts` was itself manufacturing the one SPLIT — it pointed two programs reading different proofs at `professional` — and now files them under `professor` / `self_employed` (the latter as the `amounts:'catalog'` demo) with a `resetProgramNameKey` so RESET does not trade one unsavable program for another. Seed's standing `TODO (PR 2)` closed: `validateIncomeRule` runs over every `CATALOG_INCOME_RULE` entry before writing (rejected → reported and skipped, never a half-seeded catalog), and axis 4's `JSON.stringify` compare became key-order-stable — it was rewriting every already-correct rule and logging `byProfessorRank(3 rows) → byProfessorRank(3 rows)`. Admin: the income-rule editors move to `shared/income-rule/` with one `variant` input (`'catalog'` = proof picker on, bank policy off; `'program'` = the reverse) rather than three booleans that could assemble a meaningless combination; the catalog name detail gains the block ABOVE the loan-type tabs (the rule is per NAME, not per category) with an explicit Save, because the write can be refused and an autosave that fails sits under a chip saying "Saves automatically"; step 5 becomes one bare panel + two peer choice cards reusing step 1's `basis-card` vocabulary verbatim (medallion, bloom, commit spine, stagger, focus halo, reduced-motion) with the catalog's REAL figures rendered in a `<dl>` on the card — "3 rows" says nothing about what the bank will pay. Picking "own amounts" seeds the editor from the catalog; switching back names what is lost instead of asking "are you sure". 36 new backend specs; 32 new i18n ids with ar targets, 2 orphans dropped. **Pre-existing, untouched:** the same 15 `MatchingPreviewService` ctor-arity failures.
- 2026-08-16 (v16.5.0): MINOR — **a catalog name states ONE income basis per loan type, not a set: radios, and a seed that converges on one.** v16.4.1 kept two independent booleans on the argument that a name is legitimately sold both ways by two banks. That argument is about BANKS, and since v16.4.0 a bank states its own basis on its own program (`bank_program.programType`, the only place it is enforced) while the catalog answers a different question — "what is this name FOR?" — which has one answer. Marking both said nothing, and left the seeded catalog with six names an operator could neither read as decided nor clear. **No migration, no API change, no error code:** the wire shape stays `IncomeBasis[]`, the API still accepts two, and a row already holding two still renders — as a named `mixed` state, not as silence. Admin: both surfaces that ask the question become native radio groups sharing a name — `enumeration-edit.dialog.ts` (Add / Edit program name) and the name's own page — with the tick redrawn as a circle + scale-in dot, because a rounded square teaches "as many as apply" before the first click. There is no unpick, so the empty set the API refuses is unreachable and `basisInvalid` / `basisMapValid` / `basisTouched` / `canSave` / the `Validators.required` / the detail page's `basis_last` refusal are all deleted — each guarded a state the operator could only reach by mistake. `mixed` = the loan types disagree, or one row holds both: neither radio checked (honest — the group has no single answer), accent ring without fill, plus a line naming where it IS live; picking resolves it to that basis on every offered loan type. Fixed in passing: the dialog's tick was inked `var(--text-inverse)`, a token defined nowhere, so the check fell back to body ink — dark navy on solid azure/plum. i18n: `lookups.field.incomeBasis.mixed` reworded, `pnd.basis_both` added, `lookups.field.incomeBasis.required` + `pnd.basis_last` dropped (verified against `ng extract-i18n`). Seed: `CATALOG_NO_PAYSLIP` (additive, only ever turned `noPayslip` ON) → `CATALOG_INCOME_BASIS` + pure `catalogIncomeBasis(key, category)`, DERIVED from the assignment map so a pair the catalog does not offer is unrepresentable. Three rules: `business` is never a payslip product (every seeded business program is `income_surrogate`); a self-employed archetype carries its basis across every category it reaches; a curated no-payslip FACT sells the `personal` product that way and payroll sells the rest. Rule 3 deliberately disagrees with `seed-bank-programs.ts#skeletonFor` on armed_forces / police / govt_employee — intent vs fact, which v16.4.0 made separate on purpose. `SELF_EMPLOYED_ARCHETYPES` moves to the matrix and `seed-bank-programs.ts` imports it (was two copies, A25). Axis 3 is now authoritative like axes 1 and 2 — it OVERWRITES a manual pick and logs every change with its direction (`both → no_payslip`); axis 1 drops its basis carry-across and stamps a newly created pair with the curated basis instead. Run against the dev DB: 16 pairs corrected, 43/43 now carry exactly one, re-run reports 0. New test `catalog-income-basis.spec.ts` (6). **Pre-existing, untouched:** the same 15 `MatchingPreviewService` ctor-arity failures v16.4.1 records.
- 2026-08-16 (v16.4.1): PATCH — **the catalog name's income BASIS is back on Add and Edit program name; its ENFORCEMENT stays gone.** v16.4.0 deleted the whole concept, which took the operator's only way to say what a name is FOR out with the thing that was wrong about it — the save-time rejection. Split in two: the STORAGE and the control return, the constraint does not. `payslip` + `noPayslip` are back on `platform_enumeration_loan_category` (**no migration** — `20260816090000_drop_program_name_income_basis` was never applied to any database and is deleted, so the columns never left); `PUT admin/enumerations/:id/income-basis`, `setIncomeBases`, `incomeBasisAssignments`, `incomeBasesOf`, `incomeBases` on create, `incomeBasesByCategory` on the row DTO, `EnumerationMember.incomeBases` (READ-only now — no reader in the write path), the `incomeBasis.<category>` audit key, `common/income-basis.util.ts`, the `setCategories` / `setCategoriesBulk` / seed carry-across and `CATALOG_NO_PAYSLIP` all restored, plus `ENUMERATION_CATEGORY_NOT_ASSIGNED` (ar+en) whose only thrower is the basis write. **Deliberately NOT restored:** `PROGRAM_NAME_KEY_BASIS_MISMATCH` and `assertProgramNameKey`'s basis half, the bank-program picker's basis filter, `programNameMismatch`'s `'basis'` reason and its two empty-picker messages, and `memberIncomeBases` (the uncached read that existed only to back that rejection) — a stale catalog tick must never refuse a program the bank is entitled to save, which was v16.4.0's real finding. v16.4.0's `countProgramNameUsage.byCategory` counters STAY: the catalog now carries both, and they answer different questions — the stored basis is INTENT ("what is this name for"), the counters are FACT ("what did banks do"), and they are allowed to disagree. Admin: the Add / Edit dialog asks it again — one flat answer on create, and on edit the same two rows read the stored per-loan-type map **tri-state** (`on` / `mixed` / `off`), a `mixed` row naming the loan types it IS on and resolving UP to "on everywhere" (clearing it would drop the types that already had it); a PARKED name gets a line saying why there is nothing to set; the save writes basis FIRST and only for loan types whose set moved, so a refusal leaves the row untouched and an untouched row sends nothing (editing a label can no longer flatten a per-type basis set on the name's own page). Tests `program-name-income-basis.spec.ts` restored (5); `bank-program-name-basis-unconstrained.spec.ts` (5) and `no-payslip-usage-counters.spec.ts` (10) still pass — the two together are now the regression guard for "stored, and still unconstraining". **Pre-existing, untouched:** 15 failures in `matching-preview-figures` / `matching-preview-typed-answers` / `preview-asked-set-parity` — those specs build `MatchingPreviewService` with 4 ctor args since v16.2.0 gave it a 5th (`enumerations`).
- 2026-08-16 (v16.4.0): MINOR — **the stored income BASIS on a catalog name is DELETED; the bank's own program is the only place the basis is stated, and the catalog REPORTS it.** v16.1.0 recorded, per (program name, loan category), whether the name may be sold against a payslip / without one, and `assertProgramNameKey` refused a program whose `programType` was not in that set. That made two records of one decision — the bank picks the basis in step 1 of its own program — kept in step by hand, on a screen (`/program-catalog`) most of the roles that use the program wizard cannot open. A stale tick could refuse a save the bank was entitled to make, and the fix was out of reach of whoever hit it. Migration `20260816090000_drop_program_name_income_basis` drops `payslip` + `noPayslip` from `platform_enumeration_loan_category` (re-derivable from `bank_program.programType` at any time, which is the point). Backend: `PROGRAM_NAME_KEY_BASIS_MISMATCH` + its exception deleted from both locale dictionaries, and `ENUMERATION_CATEGORY_NOT_ASSIGNED` with them (its only thrower was the basis write); `PUT admin/enumerations/:id/income-basis`, `setIncomeBases`, `incomeBasisAssignments`, `incomeBasesOf`, `memberIncomeBases`, `EnumerationMember.incomeBases`, `incomeBasesByCategory` on the row DTO, `incomeBases` on create, the `incomeBasis.<category>` audit key and `common/income-basis.util.ts` all gone; `setCategories` / `setCategoriesBulk` / `seed-program-catalog` lose the carry-across they only needed for those columns, and `CATALOG_NO_PAYSLIP` leaves the seed matrix. **Load-bearing replacement:** `countProgramNameUsage` gains `byCategory` (`{ personal: { payslip: 2, noPayslip: 1 } }`), counted in the same pass off the same `programType` as the existing totals — a category with NO program is absent, not zeroed, so a tab can say "no bank offers this yet" instead of reporting a split that does not exist; an unknown `productCategory` string is counted in the totals but keyed under no tab (A26). Admin: the name detail's two tickable rows become a read-only tag row ("How banks prove the income" → one tag per basis a bank actually picked, with its count, plus where the choice is made), its questionnaire caveat re-gated on a REAL no-payslip program (`noPayslipUsage() > 0 && !surrogateCapable()`) instead of on a mark; the catalog list's chips and `is-surrogate` accent read the counters, so a name nobody offers yet is under neither chip and the "banks sell this without a payslip but the name is not marked for it" contradiction card is gone with the thing it contradicted; Add/Edit program name stops asking the basis; the wizard's name picker stops filtering on it, `programNameMismatch` drops its `'basis'` reason and the two basis-specific empty-picker messages go. 15 dead i18n ids removed from `messages.ar-EG.xlf`, 3 added. `core/income-basis.ts` STAYS — the wizard's two step-1 cards and the bank/program read-back surfaces still speak `payslip` / `no payslip`. New tests `bank-program-name-basis-unconstrained.spec.ts` (5 — both types accepted on one name, the category half still refusing, and the success exit itself, which the removal briefly dropped), `no-payslip-usage-counters.spec.ts` +3 for the per-category split; `bank-program-name-basis-enforcement.spec.ts` + `program-name-income-basis.spec.ts` deleted. **Consequence, accepted:** the catalog can no longer stop a bank from selling a name the way it likes — the only thing that decides is the program.
- 2026-08-15 (v16.3.0): MINOR — **the operator-facing surrogate-fact surfaces are DELETED: the per-name fact tick-list and the Manage values "Income facts" rail.** Both said which figure a bank works an income out from; neither was read by anything that quotes, publishes or validates. The tick-list ("What the bank can work the income out from", on a catalog name, per category) was a THIRD claim about one thing — the bank already picks the exact method on its own program, and the questionnaire already decides whether the figure is asked — so it read as a second question list with no consequence, and its "not asked" warnings pointed at a screen the operator had to keep in step by hand. Admin: the block, its computeds (`surrogateFacts` / `factState` / `factsPicked` / `factsNotAsked` / `toggleFact` / `unboundFacts`), its styles and its 15 i18n ids are gone from `program-name-detail.page.ts` (the income-BASIS rows above it stay, and `surrogateCapable` now derives from `BUILTIN_FACT_QUESTION_CODES` instead of a registry fetch); the catalog board drops its "reads: …" line and the "no fact is picked" card state; `surrogate_fact` leaves `LOOKUP_TYPES`, taking `fact-question-picker.dialog.ts`, the per-row binding chip, `setBoundQuestion()` and `EnumerationRow.boundQuestion` with it. **Backend registry untouched** — `surrogateFactRegistry()`, `fact:<key>`, `resolveRegistryFact`, `validateFactRule`, the seeded `systemOnly` rows and `PUT :id/bound-question` all stand, so live rules keep quoting and a broken binding is still fixable by API; only the screens went. Two replacements, because deleting the ticks would otherwise have silently disabled two real warnings: (1) `EnumerationMember.noPayslipFacts` (tick-derived) is REPLACED by `BoundQuestion.askedIn` — the question's own `question_loan_category` assignment, carried on the member — and the wizard's step-4 line reads that instead, so it now answers "is this figure asked here?" from the thing the engine depends on rather than from a name's tick; (2) `noPayslipProgramsWithoutTable` drops the `noPayslipPairs()` name-promise half and becomes program-local `readsAFactWithNoTable()` — a rule whose strategy reads a fact (built-in or `fact:`) with an empty `keyTable` AND `bands`. `declared` surrogate programs stay unflagged (seven business/professional programs carry that pair on purpose); an unparseable blob now counts as 0, not 1. `INCOME_RULE_FACT_UNAVAILABLE` reworded in both locales — it told the operator to open a screen that no longer lists facts. `noPayslipFactsFor` / `sellsWithoutPayslip` / `FactPickable` deleted from `core/surrogate-facts.ts`. New test `fact-question-asked-in.spec.ts` (5) replaces `no-payslip-fact-derivation.spec.ts`; `no-payslip-usage-counters.spec.ts` re-based (7). No migration: `platform_enumeration_question` still backs the scoring-template picker, which is a different feature on the same screen. **Consequence, accepted:** adding a NEW income fact or re-pointing a broken one is no longer an admin action.
- 2026-08-15 (v16.2.0): MINOR — **the surrogate income FACT registry is DATA: an operator adds one on Manage values.** The four facts a no-payslip rule can be keyed by were a code constant (`SURROGATE_FACT_BINDINGS`) with a typed profile field, a hand-written resolver branch and a dropdown entry each, so "this bank works the income out from the taxi licence class" was a release — while the bank's TABLE, keyed by that fact, had been data since 011. Migration `20260815130000_surrogate_fact_registry`: `boundQuestionId` on `platform_enumeration` (FK to `question`, `ON DELETE SET NULL`, indexed) and the four facts INSERTed as `systemOnly` rows of the new type `surrogate_fact`, bound to the question codes the constant named. The column is on the ENUMERATION, not on `question` — A33 forbids a profile-mapping field there, and the direction here is the opposite one: the registry declares what it reads, the question stays pure content. Backend: new pure `matching/pipeline/surrogate-fact-registry.ts` (owns `BINDABLE_QUESTION_TYPES` = SINGLE_SELECT | NUMERIC, and `SurrogateFactBinding`, re-exported by the repository so Principle V's import direction holds); strategy token `fact:<key>` (`FACT_STRATEGY_PREFIX` / `factStrategy` / `factKeyOf` in `matching/types.ts`) widening `IncomeAssumptionStrategy` — a namespaced token, not a `factKey` field, because every existing reader already switches on that one string; `ApplicantProfile.surrogateFacts` (`{kind:'choice'|'numeric'}` per fact key) filled by `surrogateFactsFromAnswers(answers, registry)`, which now runs TWO passes — the four legacy facts still land in their typed fields, because live offers carry `byMilitaryGrade`-style tokens that read them by name (Principle I); `resolveRegistryFact` in the resolver (choice → key table, number → bands, unanswered → `fact_not_answered`, never a zero); `surrogateFactRegistry()` + `questionOptionCodes()` + `setBoundQuestion()` + `boundQuestions()` on the repository, all uncached where they back a quote or a rejection. Save validation: `validateIncomeRule` gains `validateFactRule` — a fact the registry cannot serve is refused with the new `INCOME_RULE_FACT_UNAVAILABLE` (422, ar+en, `meta.availableFacts`), and a choice fact's table keys are checked against the BOUND QUESTION'S OPTIONS (reported through `INCOME_RULE_UNKNOWN_KEY` with the question as the registry) so the keys the bank picks and the answers the customer picks are one list by construction. The DTO's `@IsIn(STRATEGIES)` becomes a `@Matches` shape check — the legal set is no longer knowable at compile time, which is the point. `PUT admin/enumerations/:id/bound-question` (audit key `boundQuestion`), plus `ENUMERATION_QUESTION_BINDING_NOT_APPLICABLE` and `SURROGATE_FACT_QUESTION_TYPE_INVALID` (422, ar+en). The publish check and the catalog's `noPayslipFacts` counters now read the registry instead of the constant — `SURROGATE_FACT_QUESTION_CODES` in the admin bundle is renamed `BUILTIN_FACT_QUESTION_CODES` and no longer answers "is this question a fact?". Admin: `surrogate_fact` joins the Manage values rail ("Income facts") with a per-row binding chip and a new `fact-question-picker.dialog.ts` (SINGLE_SELECT/NUMERIC only, unbind offered); the program wizard's method picker lists registry facts beside the four built-ins in ONE group, derives the editor shape from the bound question (`incomeMethodShape`), feeds the key table the question's options (`keyOptions`, so no enumeration sits between the two lists), and the rule-check panel gains a generic `factValue` sample. Not an A26 amendment and not a new category: a fact is a lookup value, and the questionnaire still decides whether it is ASKED. New tests `registry-fact-income-rule.spec.ts` (10); `no-payslip-fact-derivation.spec.ts` now supplies a registry. **Not yet done:** the read-only program detail page still renders nothing about the income rule.
- 2026-08-15 (v16.1.0): MINOR — **the income basis is STORED on the catalog name, per loan category, and the API enforces the pairing.** v16.0.0 inferred "sold without a payslip" from the surrogate fact ticks; that cost three things — the Add-program-name dialog could not state it (a new name was silently payslip-only until someone opened the fact tick-list on another screen), the payslip half of the wizard's picker was unfilterable (a no-payslip-only name still showed under "Reads a payslip"), and unticking the last fact silently un-sold the product. Migration `20260815090000_program_name_income_basis`: `payslip` + `noPayslip` booleans on `platform_enumeration_loan_category`, backfilled `noPayslip` from the existing fact ticks (6 pairs) with `payslip` left true EVERYWHERE so no currently-savable program becomes unsavable. TWO booleans, not an enum — a name is legitimately sold both ways by two banks, which is the whole reason `fast` was deleted. Backend: new `common/income-basis.util.ts`, `EnumerationMember.incomeBases`, `memberIncomeBases()` (uncached, backs a rejection), `PUT admin/enumerations/:id/income-basis` (category in body, audit key `incomeBasis.<category>`), `incomeBases` on create (default `['payslip']`), and `setCategories`/`setCategoriesBulk`/`seed-program-catalog` now CARRY the flags across their delete-then-insert. `assertProgramNameKey` gains the basis half after the category check — new `PROGRAM_NAME_KEY_BASIS_MISMATCH` (422, ar+en) grandfathered on an unchanged (name, basis) pair, plus `ENUMERATION_CATEGORY_NOT_ASSIGNED` for a basis write on an unoffered pair. `noPayslipFacts` KEPT and re-scoped: it now answers only "which figure does the table read, and is it asked" (the step-4 warning). Admin: Add-program-name asks the basis as two tickable rows; the name detail replaces the derived "Sold without a payslip" switch with the same two rows, autosaved (fact ticks are no longer deleted when it goes off); the catalog list's chips read the stored field and are MEMBERSHIP, not a split (a both-ways name appears under both); the wizard filters its picker in both directions and its mismatch signal now matches what the API refuses. Orphan `PROGRAM_TYPE_INVALID_FOR_CATEGORY` deleted from both locale dictionaries (v16.0.0 missed them). New tests `bank-program-name-basis-enforcement`, `program-name-income-basis`. Not an A26 amendment: no category, no hardcoded capable list — the questionnaire still decides whether a fact is ASKED.
- 2026-08-14 (v16.0.0): MAJOR — **`fast` removed; no-payslip is an income BASIS, not a loan category.** Scope-lock back to four (A26 re-aimed at a fifth, and at re-introducing no-payslip as a category). Prisma `LoanCategory` back to four + the unapplied `20260814110000_fast_loan_category` migration DELETED — the value never reached any database, so there is no destructive migration and nothing to wipe. **Both category sets deleted, not renamed:** capability is now derived from whether a category's applicants are asked one of the four facts (`question_loan_category`), admin-configurable on `/questionnaire/categories`; `personal` + `car` are the seeded default. `PROGRAM_TYPE_INVALID_FOR_CATEGORY` + `ProgramTypeInvalidForCategoryException` deleted across backend + both locale dictionaries. `collectIncomeRuleWarnings` drops the category term and keys off `programType` alone (matching `quote.ts#shouldConsultIncomeRule` — it previously reported a rule as ignored on categories the engine WOULD price off). Publish warning `not_assigned_to_surrogate_categories` → `not_asked_by_any_category`. **Load-bearing fix:** the catalog usage counters gated on the no-payslip CATEGORY, so removing it would have silently zeroed the board's only actionable warning — re-gated on `programType`, `fastPrograms*` → `noPayslipPrograms` / `noPayslipProgramsWithoutTable` through repo → DTO → controller → Angular client. New derived `EnumerationMember.noPayslipFacts` (facts ticked per category) so the program wizard can filter its name picker by basis and warn before saving when the fact a method reads is not set up. New `admin/src/app/core/income-basis.ts` (owns `ProgramType` + the operator-facing words; `income_proof`/`income_surrogate` are never rendered) and derivations in `core/surrogate-facts.ts`. Admin UI: catalog list is ONE grid with `All · Reads a payslip · No payslip` chips (lanes gone; a name sold both ways renders once, saying both); catalog name detail gains a "Sold without a payslip" switch on EVERY category tab, with the fact states split into independent `picked`/`asked` booleans (a ticked-but-unasked fact used to read as plain "not asked" and hid a live tick); the wizard replaces the Program-type dropdown with two step-1 choice cards, filters the name picker to fact-carrying names, leads step 4 with the rule, groups the eleven methods by what they read, adds the fact-binding line, and adds an income row to the review step. `--color-cat-fast` → `--color-income-surrogate`. Flutter: enum member, home card + branch, `pages/fast/`, route and ARB keys removed, router + l10n regenerated. New tests `no-payslip-usage-counters`, `no-payslip-fact-derivation`; `fast-loan-program-type.spec.ts` deleted. **Not yet done:** the read-only program detail page still renders nothing about the income rule, and `warnings[]` is still read by no screen.
- 2026-08-14 (v15.1.0): MINOR — **surrogate-CAPABLE ≠ surrogate-REQUIRED.** A personal or auto loan may also be sold with no payslip, so `personal` + `car` join `fast` as categories where a surrogate program is legitimate. Backend: `SURROGATE_LOAN_CATEGORIES` / `isSurrogateCategory` → `SURROGATE_REQUIRED_CATEGORIES` / `requiresSurrogateProgramType` (renamed, NOT aliased — the old short name was reached for by both meanings), new `SURROGATE_CAPABLE_CATEGORIES` / `isSurrogateCapableCategory` in `common/loan-category.util.ts`. `collectIncomeRuleWarnings` NARROWS the category term to the capable set instead of dropping it (`productCategory === 'personal'` was falsely reporting a car surrogate program's rule as ignored while `quote.ts` read it; a grade table on a mortgage still warns). `SurrogateFactSpec.category` dropped; `collectSurrogateBindingWarnings` is now a set difference over the capable set with `missingCategories` in the meta (`reason: 'not_assigned_to_surrogate_categories'`). Seed references `MILITARY_GRADE_Q` / `ACADEMIC_RANK_Q` / `YEARS_IN_PRACTICE_Q` under `CAR` too — the card-limit fact already rode the obligation block — so **`npm run seed:questionnaire` (write assignments THEN publish) is required** before car applicants are asked. Admin: catalog fact tick-list gated on `isSurrogateCapableCategory` (Personal / Auto Loan / Fast Loans tabs) with the three Fast-specific strings re-worded off `categoryName()` (ar targets updated); the catalog list's two lanes + "Need a bank table" counter stay on the REQUIRED set on purpose (the capable set would put nearly every name under a "Fast Loans — no payslip" heading). Mobile `car_apply_mapper.dart` sends the facts like the personal one (consistency — the backend derives them from `questionnaireAnswers` either way). No migration, no new error code, not an A26 amendment.
- 2026-08-14 (v15.0.0): MAJOR — **fifth loan category `fast` = "Fast Loans", the no-payslip product.** Constitution scope-lock widened from four to five (A26 re-aimed at a sixth). Prisma `LoanCategory` + `20260814110000_fast_loan_category` (ADD VALUE, one-way). Backend: `ALL_LOAN_CATEGORIES` + `SURROGATE_LOAN_CATEGORIES` / `isSurrogateCategory` in `common/loan-category.util.ts` (new `asLoanCategory` too), category-array DTO caps derived from the list instead of a literal `4`, and a new rule — a `fast` program must be `income_surrogate` (`PROGRAM_TYPE_INVALID_FOR_CATEGORY`, 422, ar+en); the converse stays legal because business/professional programs carry that type with `strategy: 'declared'`. Admin: Fast Loans category label + colour token, its own tab on a catalog name with the SURROGATE-FACT block (which fact the bank's table reads, and whether it is asked at all), its own section on the catalog list. Questionnaire: `fast` category config asking the four facts. Rejected alternative, for the record: modelling it as an income LANE inside `personal` — cheaper, but the questions asked would then depend on a program attribute the customer never chose.
- 011-surrogate-admin-panel: Added Node.js 22 LTS + TypeScript 5.6 (`strict`, `noUncheckedIndexedAccess`) · + NestJS 10, Prisma 5, `class-validator`, `@nestjs/swagger`, `decimal.js`
- 2026-08-06 (v14.0.0): MAJOR — **Principle V: every question type is scoreable.** Formula unchanged; only the derivation of one answer's 0–100 score is now type-aware (option score · multi-select `aggregation` · numeric `[from, to)` bands · text presence). Fixes the biggest match-quality gap: `SCOREABLE_TYPES` was `['SINGLE_SELECT']`, so monthly income, existing debts, requested amount and term priced the loan but never moved the match, and both preview and apply dropped every non-single-choice answer before the scorer. Backend: `SelectedAnswer` → discriminated union (`option`/`options`/`numeric`/`text`), new pure `answerScoreFor` + `bandFor`, factor breakdown re-pointed at the same function, one shared `matching/scoring/answer-to-selected.ts` for both paths, `normalizeWeights` gains `multiSelectRules`/`numericBands`/`textRules` (absent on old rows → no migration) and detects the new shape by key presence, not truthiness. `saveWeights` enforces a rule per weighted question: new codes `WEIGHTS_MISSING_RULE`, `WEIGHTS_NUMERIC_BANDS_INVALID`, `WEIGHTS_RULE_TYPE_MISMATCH` (422, ar + en); `QUESTION_TYPE_NOT_SCOREABLE` retired-but-retained. `listWeightableOptions` returns all four types + numeric bounds/unit. Admin: type-driven step 3 + new shared `app-score-bands-editor` (edges only → gaps/overlaps unrepresentable; `scoreBandsErrorFor` mirrors the backend) + aggregation select + text presence score. Seed emits the new shapes. New tests `numeric-band-scoring`, `multi-select-aggregation`, `text-presence-scoring`, mixed-type cases in `asked-weight-denominator` + `preview-asked-set-parity`. Mobile unchanged. A33 extended. Walkthrough: [specs/questions-and-weights-flow.md](specs/questions-and-weights-flow.md).
- 2026-08-06 (v13.0.0): MAJOR — **Principle V: the score is normalised over the questions the applicant was ASKED**, not a flat 100. `probability = Σ_answered(questionWeight × answerScore÷100) ÷ Σ_asked(questionWeight)`. Fixes two defects: programs weighting different question sets were ranked on incomparable scales, and weight on a question the program's own category does not ask silently capped that program below 100% forever (the two assignment axes are set on different screens and nothing compared them). Backend: `computeProbability(scoring, answers, askedQuestionCodes)` + exported `askedWeightSum`; `resolveAnswers` → `{ resolved, askedQuestionCodes }`; `isQuestionVisible` promoted to shared `questionnaire/validation/question-visibility.ts` so preview and apply cannot drift; preview now scopes to the requested category (it read the whole pool while apply scoped). Same PR: new `bank_offer.approvalUsedDefault` column (migration `offer_approval_used_default`) threaded through apply / admin / saved-offer responses — an unrated program must not read as a bad fit, and configuring it later would otherwise rewrite an immutable offer's meaning (Principle I). Admin scoring editor: per-row "not asked" tag + summary warning (warn, never block); `questionsWithOptions()` selects `categories`. Mobile: **"{pct}% Guarantee Approval" → "{pct}% match"**, "{pct}% Approval" → "{pct}% match score", plus "Not rated yet", on all four surfaces — the number has never been compared to a real bank decision, so it is not worded as a guarantee or a probability. New tests `test/unit/asked-weight-denominator.spec.ts`. A33 reworded. No new error codes.


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
